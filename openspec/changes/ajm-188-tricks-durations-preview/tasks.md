# Tasks

Пункт — один воркер, один PR; id в конце строки — issue пункта, она же находка (`Closes` в PR). Задачи уже заведены (AJM-62, 60, 61) — `tools/pm/tasks-to-linear.py` их пропустит. Проверка: хост-тесты, падающие до починки; поведение после — по дельте `specs/app/tricks/spec.md`; `openspec validate ajm-188-tricks-durations-preview --strict`; `CONFORMANCE=required tools/test-all.sh`. Порядок обязателен: 1.1 → 1.2 → 1.3, параллельно ничего.

## 1. Приложение

- [ ] 1.1 Геометрия и кинематика трюков переезжают в хост-тест: `app/tests/tricks/main.swift` с `sources` = `Tricks.swift`, `TrickSim.swift`, `ControlModel.swift` по образцу `app/tests/intent`; утверждения переносятся из `app/AJMiddleCarTests/TricksTests.swift` и `TrickSimTests.swift` — обратимость пончика (N кругов → N оборотов, радиус D/2), разворот за T, замыкание восьмёрки, ограничения (медленный борт не назад, разворот не быстрее полного хода), вырождение (нулевая скорость → 0, без NaN); заголовок `Tricks.swift` про «WS channel» поправить — канал UDP `rt`; проверить `tools/test-all.sh` — каталог компилируется и проходит, число утверждений до и после назвать в описании PR, XCTest-копии удалять только после зелёного хост-теста; источники: `app/tests/intent/` (образец), `tools/test-all.sh:3-6,31` (AJM-62)
- [ ] 1.2 Длительность в списке — та, что сыграет, и ручные длительности становятся вторым режимом трюка: `TricksSettingsView.totalSec` считает через `ControlIntent.build` с теми же номиналами, что воспроизведение; у трюка появляется режим сборки (`геометрия` по умолчанию / `вручную`), хранится в `TrickSettings` рядом с остальными настройками и переживает перезапуск; `TrickEditorView` получает переключатель режима и показывает ветку `controls` (`row(i)`) в ручном режиме; `ControlIntent.build` ветвится по режиму, а не только по id — ветка `default` становится достижимой; `TrickSimView` в ручном режиме перестаёт игнорировать `durs`; проверить хост-тестом из 1.1: «Разворот» в списке и `ControlIntent.build(...).totalMs` дают одно число при умолчаниях (сегодня 5,0 с против 3,0 с), смена режима меняет сборку, `clampDur` держит 100…10000 мс, ни один шаг не неконечен; читать раздел «Мёртвая ветка — это не дверь» в `design.md` до начала; источники: `app/AJMiddleCar/TricksSettingsView.swift:42-44`, `TrickEditorView.swift` (ветка `controls`), `TrickSettings.swift:7-27`, `ControlIntent.swift:107-127`, `Tricks.swift:137-172` (AJM-60)
- [ ] 1.3 Предпросмотр считает по номиналу там же, где воспроизведение: `TrickSimView.vmaxMS` при неизвестном колесе или несовпавшем пресете возвращает `Tricks.donutNominalVmaxMS`, как `ControlIntent.vmax`, и предпросмотр показывает числа с пометкой, что скорость номинальная (вместо «Выберите модель мотора для расчёта» без чисел); колея в обоих местах уже берётся с запасным значением — не трогать; `README.md:75-76` («previews exactly what will be streamed») становится правдой — проверить, что строка всё ещё верна, и поправить, если нет; проверить хост-тестом: при несовпавшем пресете предпросмотр и `ControlIntent.build` дают одинаковые шаги и одинаковый `totalMs`; источники: `app/AJMiddleCar/TrickSimView.swift:29-56`, `ControlIntent.swift:93-99`, `README.md:75-76` (AJM-61)
