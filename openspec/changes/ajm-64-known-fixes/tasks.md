# Tasks

Каждый пункт — одна dev-задача (issue в Linear-проекте эпика, один worktree, один PR). Общая проверка: тест, который падал бы до починки, в хост-наборе; `openspec validate ajm-64-known-fixes --strict`; `CONFORMANCE=required tools/test-all.sh` зелёный; diff PR — только файлы своего пункта. Поведенческие пункты (1.x, 2.x) сверяются с дельтой `specs/<capability>/spec.md` этого change: код после починки делает то, что там написано. Исходный issue — источник `file:line`; закрывать его строкой `Closes AJM-N` в PR рядом с `Closes` задачи. Пункты `(стенд)` — человеку с платой, в Linear не уходят.

## 1. Прошивка машинки

- [ ] 1.1 `POST /ota`: не-образ на первом блоке → `400 not_firmware`, отказ флеша → `500 write_failed` (AJM-37): карта «код IDF → код конверта и статус» чистой функцией с хост-тестом, `ota_api.c` только вызывает её; проверить `make -C firmware/car/core/test run` с новым тестом карты и `tools/conformance.py` против мока (шаг с 4096 нулями); источники: `firmware/car/core/main/ota_api.c:77-86`, `tools/conformance.py:500`, `tools/mock_car/state.py` (ответ на не-образ), дельта `specs/car/ota-and-rollback/spec.md` (AJM-65)
- [ ] 1.2 Видеоподписка завершается при смене владельца rt-сессии (AJM-40): подписка помнит sid открытия, на каждом такте сравнивает с `rt_link_owner_sid()`, не совпало — конец подписки и потока; правило в `video_sub.h`, тест в `test_video_sub.c`; проверить `make -C firmware/car/core/test run` и `tools/conformance_video.py` против мока; источники: `firmware/car/core/main/video_sub.h:29-63`, `video_link.c` (такт управления), `rt_link.h` (`rt_link_owner_sid`), дельта `specs/car/video-stream/spec.md` (AJM-66)

## 2. Приложение

- [ ] 2.1 Обязательный мастер калибровки открывается по первому кадру с `motors.calibrated:false`, не только по смене (AJM-63): чистое правило «мастер обязателен» от кадра и часов в отдельном модуле, вычисляется на каждом кадре, `DriveView` показывает результат; память прежней сессии не участвует; проверить хост-тестом правила в `app/tests/` (первый кадр `false` → true; `true` → false; окно двух секунд после сохранения) и в симуляторе против мока с `calibrated:false`; источники: `app/AJMiddleCar/DriveView.swift:181-200`, `CarLink.swift` (`lastTelemetry`), `DriveMode.swift` (образец чистого правила), дельта `specs/app/calibration/spec.md` (AJM-67)
- [ ] 2.2 Выпуск принимается только с образами обеих плат (AJM-56): `latestReleaseLookup` проверяет оба имени активов, `.noImage` называет отсутствующую плату, `fetchRelease` держит стадию с ней в тексте; проверить хост-тестами `app/tests/update` (выпуск без образа машинки на стадии адаптера → удержание с платой «машинка») и `app/tests/stagerule`; источники: `app/AJMiddleCar/AppFlow.swift:223-262`, `UpdateClient.swift` (`latestReleaseLookup`), `UpdateRules.swift` (`Device.assetName`), дельты `specs/shared/release/spec.md`, `specs/app/launch-ladder/spec.md` (AJM-68)
- [ ] 2.3 Экран прошивки показывает `code` конверта локализованной фразой, `message` — в журнал; транспортные ошибки адаптера — без цитаты (AJM-57, AJM-58): фраза на каждый код `errors` обоих контрактов в `L`, единая классификация «конверт / транспорт» для обеих плат; проверить хост-тестом «ошибка клиента → текст отказа» для машинки и адаптера; источники: `app/AJMiddleCar/FirmwareFlow.swift`, `FirmwareView.swift`, `UpdateClient.swift`, `DongleClient.swift`, `L.swift`, `contract/car-api.json` → `errors`, `contract/dongle-api.json` → `errors`, дельта `specs/app/firmware-update/spec.md` (AJM-69)

## 3. Тесты и мок

- [ ] 3.1 Три проверки `rebootWindow` в `app/tests/update` стоят после вердикта и не могут провалить прогон (AJM-59): перенести вердикт в конец; проверить, что искусственно сломанная проверка роняет `tools/test-all.sh`; источники: `app/tests/update/main.swift:164-173` (AJM-70)
- [x] 3.2 Мок догоняет прошивку: срабатывание сторожевого таймера потребляет крошки, начало OTA не снимает взвод (AJM-32, AJM-38); проверить новыми тестами в `tools/mock_car/test_state.py` (второй возврат после срабатывания ничего не отматывает; `link.timeouts` растёт при тишине под OTA) и `python3 tools/mock_car/test_state.py`; источники: `tools/mock_car/state.py` (`_trip`, начало OTA), `firmware/car/core/main/recovery.c` (`snapshot_consume`), `rt_glue.h` (AJM-71)
- [ ] 3.3 `firmware/dongle/verify-on-host.sh` ходит в `/net`, конечная точка — `/wifi` (AJM-44); проверить `bash -n` и сухим чтением, что все три шага скрипта бьют в `/wifi` с телом по `contract/dongle-api.json` → `wifi_request`; источники: `firmware/dongle/verify-on-host.sh:54-57`, `firmware/dongle/main/net_api.c` (AJM-72)

## 4. Документы

- [ ] 4.1 `docs/protocol.md` догоняет код и починки (AJM-30, AJM-34, AJM-35, AJM-36, AJM-41, AJM-42, AJM-53; абзац о вытеснении — под 1.2): `rx_hz` — принятые команды; личность проверяет гейт по `/version`, не `hello_ack`; образ радио едет внутри образа машинки; `field` для элементов массива — `wheels[i]`; шесть слотов и 3 мс; `proto` в теле `POST /config` отклоняется, пример `video` с `enabled`; гейт по `proto` не решает; проверить, что `tools/check_contract.sh` зелёный (генерируемая таблица не тронута) и каждый из семи issues закрыт строкой в PR; источники: сами issues (в каждом — строки документа), `openspec/specs/car/*/spec.md` как правда (AJM-73)
- [ ] 4.2 README, CLAUDE.md, README адаптера и модема, bringup, README мока, комментарии и логи догоняют код (AJM-33, AJM-43, AJM-45, AJM-46, AJM-48, AJM-49, AJM-52, AJM-54, AJM-55): `radio.state`; правило «та же сеть» и `/net` → `/wifi` в логах и комментариях семи файлов; панель и пины по `board.h`, пять страниц; число экранов панели; провод v2, шесть доменов, три прогона в README мока; приоритет видео-реле 6; обрезка кадра — на машинке; стражи после гейта по коду; проверить `grep -rn '/net\b' firmware/dongle` пуст, `CONFORMANCE=required tools/test-all.sh` зелёный (комментарии в `.c` — часть diff), каждый issue закрыт строкой в PR; источники: сами issues, `openspec/specs/dongle/*/spec.md`, `openspec/specs/app/*/spec.md` (AJM-74)
- [ ] 4.3 `doc`-строки контрактов: `groups.link.rx_hz` — принятые команды (AJM-30), `groups.relay.to_phone_hz` — датаграммы канала реального времени (AJM-51), `video.fps` — на выходе кодера (AJM-41); правкой JSON и генератором; проверить `python3 tools/gen_contract.py && tools/check_contract.sh` и что diff сгенерированных файлов — только комментарии; источники: `contract/car-api.json`, `contract/dongle-api.json`, `tools/gen_contract.py`, `tools/gen_dongle.py` (AJM-75)

## 5. Стенд

- [ ] 5.1 Подтвердить 1.1 на живой машинке (стенд): `tools/conformance.py http://192.168.7.1` через адаптер проходит шаг с не-образом (`400 not_firmware`), рабочий образ цел; проверить, что после прогона `/version` тот же и `rolled_back:false`
- [ ] 5.2 Подтвердить 1.2 с двумя пультами (стенд): второй пульт забирает сессию — картинка у первого гаснет в пределах такта, у второго появляется с ключевого кадра после его первого `view`; проверить по `video.state` и `video.fps` в `/status` и по экранам обоих пультов
