# Tasks

Пункт — один воркер, один PR; id в конце строки — issue пункта, она же находка (`Closes` в PR). Задачи уже заведены (AJM-155, 165, 167, 168) — `tools/pm/tasks-to-linear.py` их пропустит, дублей не будет. Общая проверка: тест, падающий до починки; поведение после — по дельте `specs/shared/mock-and-conformance/spec.md`; `openspec validate ajm-187-mock-parity-tail --strict`; `CONFORMANCE=required tools/test-all.sh`. В каждой issue есть раздел «направление починки» с `file:line` — читать его, а не угадывать. Стенда не требует ни один пункт. 1.1 → 1.2 последовательно (один файл), 1.3 и 1.4 параллельно всему.

## 1. Мок и генератор

- [ ] 1.1 `calib_save` мока проверяет чужие ключи раньше недостающего `wheels` — порядок `calib_api.c:118-126`; проверить тестом в `tools/mock_car/test_http.py` рядом с `TestBodyLimits`: `POST /calibration {"foo":1}` → `400 unknown_field` с `field: foo` (сегодня `missing_field wheels`), `POST /calibration {}` → `missing_field wheels` без изменений, `{"wheels":[…],"foo":1}` → `unknown_field foo` без изменений, `/calibration/spin {"foo":1}` → `unknown_field` как и был; шаг в `tools/conformance.py` — по желанию; источники: `tools/mock_car/mock_car.py:284-299`, `firmware/car/core/main/calib_api.c:118-126` (AJM-167)
- [ ] 1.2 Рантайм-состояние мока уезжает из `request.app[...]`: `no_version` — в объект состояния рядом с `car.fw`/`car.rollback`, остальные строковые ключи (`car`, `link`, `lock`, `rollback_mode`) — на `web.AppKey`, объявленные один раз рядом с `build_app`; проверить `tools/mock_car/.venv/bin/python -W error::DeprecationWarning tools/mock_car/test_http.py` — зелёно (сегодня падает на `ota`), и `--no-version`: `/version` → `404`, после первой принятой прошивки → `200` с пятью полями, без `500`; провод не меняется; источники: `tools/mock_car/mock_car.py` (`ota`, `build_app`), `state.py` (`CarState`) (AJM-155)
- [ ] 1.3 Эмиттер валидатора `/config` в `tools/gen_contract.py`: конечное число вне int32 — `wrong_type` до проверки диапазона; для `fixed` — `scaled` вне `[-2**31, 2**31-1]`, для `int`/`enum` — `int(v)` вне тех же границ; проверить `tools/test_gen_contract.py` тремя телами из issue (`{"ramp":{"rise_ms":1e300}}`, `{"wheel":{…,"gear_ratio":1e300}}`, `{"ramp":{"rise_ms":2147483648.0}}`) — все три `wrong_type`, и `python3 tools/gen_contract.py && tools/check_contract.sh`; `tools/mock_car/generated.py` перегенерировать, не править руками; источники: `tools/gen_contract.py` (`VALIDATE_SRC`), `firmware/car/core/main/cfg_value.h` (`cfg_value_from_double`), `cfg_api.c:179` (AJM-165)
- [ ] 1.4 Видеопорт мока учится струйке: кольцо на шесть слотов и отправитель с шагом 3 мс, как `video_link.c` (`RING_SLOTS`, `SEND_PERIOD_US`); сторона энкодера остаётся тактом `VIDEO["fps"]` и кладёт access unit в кольцо, кадр без свободного слота пропускается и считается отдельным счётчиком; обе константы — в `tools/mock_car/test_mirrors.py` регулярным выражением по `video_link.c`; `judge_pacing` в `tools/conformance_video.py` перестаёт зависеть от `is_loopback`, ветка `skipped: … measured, not judged` уходит; проверить `tools/conformance_video.py 127.0.0.1` против мока — строка `keyframe pacing: median ≈ 3.00 ms` без `skipped`, остальные проверки зелёные, потерь кадров нет; `tools/mock_car/test_video.py` закрепляет шаг и отсутствие пропусков на штатном клипе; наивная `call_later` на чанк без кольца перемешивает кадры — так не делать (проверено при AJM-162); источники: `tools/mock_car/video.py` (`tick`, `run`), `firmware/car/core/main/video_link.c:27-53`, `tools/conformance_video.py` (`check_pacing`, `judge_pacing`, `is_loopback`) (AJM-168)
