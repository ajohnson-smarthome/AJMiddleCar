# Tasks

Пункты `(PM)` делает PM-сессия, в Linear они не уходят. Каждое путешествие (2.x) — одна dev-задача: воркер в worktree, один PR, в котором только `review/<nn>-<slug>.md` и его строка здесь. Проверка у всех 2.x одна: трасса с шестью разделами по `design.md`; у каждого вердикта `✗` есть issue в проекте эпика с `[J<n>]`, весом и `file:line`; у каждого `?` — строка в `## Стенд`; `openspec validate ajm-64-logic-review --strict` проходит; `CONFORMANCE=required tools/test-all.sh` зелёный; в diff PR нет ничего вне `review/` и `tasks.md`. Спеки — `openspec/specs/<область>/<capability>/spec.md`, замысел — `docs/superpowers/specs/`, обещание — `docs/protocol.md` и `contract/*.json`. Трюки — вне маршрутов.

## 1. Триаж найденного

- [x] 1.1 (PM) Триаж `AJM-30…59`: перенести в проект эпика, каждому вес и ярлык `Bug`/`Improvement`, снять дубли, пометить комментарием «→ ajm-64-known-fixes» те, что чинятся параллельным change'ем; проверить `orca linear list-issues --team AJM --json` — ни одного из тридцати вне проекта и без приоритета

## 2. Путешествия

- [ ] 2.1 Трасса J1 `review/01-first-launch.md` — первый запуск с нуля: адаптер в телефон → приложение находит адаптер → `POST /wifi` → присоединение → гейт адаптера (`/version`, выпуск, обновление, перезагрузка, откат) → машинка достигнута → её гейт → образ радио при первой загрузке новой прошивки → «другая машинка» → некалиброванная машинка и обязательный мастер → первая езда; отдельно: запрет локальной сети, выпуск без образа одной из плат, `motors.bus: down` на первом включении; проверить по общему правилу; capabilities: `app/launch-ladder`, `app/firmware-update`, `app/calibration`, `dongle/usb-network`, `dongle/car-join`, `dongle/api`, `car/radio-and-network`, `car/ota-and-rollback`, `car/status-and-version`, `car/calibration`, `shared/release`; источники: `app/AJMiddleCar/AppFlow.swift`, `StageRule.swift`, `GateRule.swift`, `VersionRule.swift`, `CarReach.swift`, `FirmwareFlow.swift`, `firmware/dongle/main/net_api.c`, `wifi_sta.c`, `firmware/car/core/main/main.c`, `radio_ota.c`, `docs/superpowers/specs/2026-09-17-board-ladder-design.md`, `2026-09-16-one-release-gate-design.md`, `2026-08-31-radio-in-one-image-design.md` (AJM-76)
- [ ] 2.2 Трасса J2 `review/02-drive.md` — обычная поездка без трюков: сессия открыта → езда → видео включить/выключить кнопкой → лист настроек поверх (подписка, команды, телеметрия) → мастер калибровки поверх → уход в фон и возврат → потеря/восстановление декодера → намеренное отключение; на каждом переходе: что с сессией, подпиской, командами, крошками; проверить по общему правилу; capabilities: `car/rt-link`, `car/video-stream`, `car/config`, `app/drive-session`, `app/drive-hud`, `app/settings`, `app/screens-and-theme`, `dongle/relay`; источники: `app/AJMiddleCar/CarLink.swift`, `DriveView.swift`, `DriveMode.swift`, `VideoLink.swift`, `SessionPolicy.swift`, `AJMiddleCarApp.swift`, `firmware/car/core/main/rt_link.c`, `video_link.c`, `video_sub.h`, `firmware/dongle/main/relay_udp.c`, `rate_gate.c`, `docs/superpowers/specs/2026-09-14-fpv-video-design.md`, `2026-09-15-drive-hud-design.md`, `2026-09-15-video-switch-design.md` (AJM-77)
- [ ] 2.3 Трасса J3 `review/03-link-loss.md` — потеря и возврат связи: тишина пульта → сторожевой таймер → возврат по крошкам → поток возобновился / не возобновился → простой сессии; тишина машинки для пульта (~3 с) → новый `hello` → список умерших sid; второй пульт (осцилляция из `docs/IDEAS.md` п. 11); адаптер потерял AP машинки на ходу (uplink, переприсоединение, что видит пульт и что делает машинка); адаптер выдернут на ходу; машинка перезагрузилась на ходу; согласованность бюджетов 300 мс / 2,4 с / 3 с / 10 с / бюджет попыток адаптера; проверить по общему правилу; capabilities: `car/rt-link`, `car/recovery`, `car/actuator-arbiter`, `app/drive-session`, `dongle/car-join`, `dongle/relay`, `dongle/usb-network`, `dongle/panel`; источники: `firmware/car/core/main/rt_link.c`, `rt_glue.h`, `recovery.c`, `link.c`, `app/AJMiddleCar/CarLink.swift`, `LinkState.swift`, `CarReach.swift`, `firmware/dongle/main/uplink.c`, `wifi_state.c`, `udp_sess.c`, `tools/mock_car/test_rtlink.py` (`TestTwoPhones`), `docs/superpowers/specs/2026-08-21-link-layer-rearchitecture.md`, `2026-06-14-link-loss-auto-return-design.md` (AJM-78)
- [ ] 2.4 Трасса J4 `review/04-update-rollback.md` — обновление и откат: выпуск в ленте → адаптер обновляется → перезагрузка → машинка обновляется → её перезагрузка → образ радио внутри окна ожидания пульта → `rolled_back` на каждой из плат и что предлагает пульт → повтор → образ с неверной сигнатурой (`AJM-37`) → выпуск без образа платы (`AJM-56`) → кэш образов и лента недоступна → приложение старше плат; проверить по общему правилу; capabilities: `shared/release`, `app/firmware-update`, `app/launch-ladder`, `car/ota-and-rollback`, `car/radio-and-network`, `car/status-and-version`, `dongle/api`; источники: `tools/release.sh`, `app/AJMiddleCar/FirmwareFlow.swift`, `UpdateClient.swift`, `UpdateRules.swift`, `GateRule.swift`, `firmware/car/core/main/ota_api.c`, `main.c`, `radio_ota.c`, `radio_flash.c`, `firmware/dongle/main/ota_api.c`, `docs/superpowers/specs/2026-08-23-ota-fix-decisions.md`, `2026-06-09-ota-update-design.md`, `2026-06-14-forced-update-gate-design.md` (AJM-79)
- [ ] 2.5 Трасса J5 `review/05-config-calibration.md` — конфигурация и калибровка: шесть доменов от чтения до записи → кэш пульта vs машинка после её перезагрузки, после стирания NVS, `storage.reset_at_boot` (пульт не читает) → `POST` подмножества и отказ записи → таблица калибровки, `inverted`, мастер по требованию (`AJM-63`) и по желанию → импульс во время езды и во время возврата → сохранение при обрыве сессии; проверить по общему правилу; capabilities: `car/config`, `car/calibration`, `car/drive`, `car/status-and-version`, `app/settings`, `app/calibration`, `app/drive-session`; источники: `firmware/car/core/main/cfg_api.c`, `cfg_json.c`, `calibration.c`, `calib_api.c`, `car.c`, `app/AJMiddleCar/ConfigStore.swift`, `ConfigState.swift`, `SettingsView.swift`, `CalibrationView.swift`, `CalibClient.swift`, `DriveView.swift`, `docs/superpowers/specs/2026-06-17-json-everywhere-design.md`, `2026-06-09-calibration-redesign-design.md`, `2026-06-16-settings-groups-design.md` (AJM-80)
- [ ] 2.6 Трасса J6 `review/06-degraded-hardware.md` — отказы железа: шина I2C down при загрузке и на ходу → нет сенсора камеры → радио `mismatch` / `unavailable` → USB адаптера отвалился → панель отсутствует → адаптер без сети машинки → для каждого: что говорит `/status`, что показывает пульт, что на панели, остаётся ли плата обновляемой и есть ли выход без перезагрузки; проверить по общему правилу; capabilities: `car/actuator-arbiter`, `car/status-and-version`, `car/radio-and-network`, `car/video-stream`, `dongle/usb-network`, `dongle/api`, `dongle/panel`, `app/launch-ladder`, `app/drive-hud`, `app/drive-session`; источники: `firmware/car/core/main/link.c`, `pca9685.c`, `camera.c`, `status_api.c`, `telemetry.c`, `wifi_ap.c`, `firmware/dongle/main/usb_net.c`, `status_json.c`, `screens.c`, `app/AJMiddleCar/LinkState.swift`, `DriveView.swift`, `ConnectView.swift`, `docs/bringup.md` (наблюдённое), `docs/superpowers/specs/2026-09-01-dongle-display-design.md` (AJM-81)
- [ ] 2.7 Трасса J7 `review/07-safety.md` — безопасность под током: каждый источник у арбитра (`rt`, `console`, `calib`, `recover`, `ota`, `safe`) × каждое прерывание (потеря связи, `bye`, старт OTA, импульс калибровки, вытеснение, перезагрузка, отказ записи в PCA9685) — ни шага, при котором моторы делают не то, что последняя команда: shoot-through, разгон при передаче владения, залипшая скважность, тень канала после отказа, состояние PCA9685 через перезагрузку P4, `motors.bus: down` при езде; проверить по общему правилу; capabilities: `car/drive`, `car/actuator-arbiter`, `car/recovery`, `car/calibration`, `car/ota-and-rollback`, `car/rt-link`; источники: `firmware/car/core/main/link.c`, `link.h`, `motors.c`, `ramp.c`, `pca9685.c`, `car.c`, `recovery.c`, `rt_glue.h`, `ota_api.c`, `test/test_link.c`, `test_motors.c`, `docs/superpowers/specs/2026-08-21-link-layer-rearchitecture.md`, `2026-08-22-audit-fix-decisions.md`, `docs/research/2026-06-12-audit.md` (AJM-82)
- [ ] 2.8 Трасса J8 `review/08-mock-blind-spots.md` — что скрывает мок: по каждой спеке `car/*` — какие требования мок исполняет иначе или не исполняет (образец — `AJM-32`, `AJM-38`), какие сценарии conformance не спрашивает, что `tools/test-all.sh` не гоняет (образец — `AJM-59`); результат — таблица «требование → мок → conformance → вердикт» и issues на расхождения, которые пульт против мока не заметит, а против машинки — заметит; проверить по общему правилу; capabilities: `shared/mock-and-conformance`, `shared/contract`, все `car/*`; источники: `tools/mock_car/mock_car.py`, `state.py`, `rt_link.py`, `video.py`, `tools/conformance.py`, `conformance_rt.py`, `conformance_video.py`, `tools/test-all.sh`, `tools/mock_car/README.md` (AJM-83)

## 3. Сведение

- [ ] 3.1 (PM) Собрать `review/00-bench.md` из разделов `## Стенд` всех восьми трасс плюс три проверки из `docs/IDEAS.md` (реальный OTA с откатом на битом образе, watchdog на ходу, апп на телефоне); проверить: у каждой строки — ожидаемый результат и какие issues она закрывает
- [ ] 3.2 (PM) Триаж находок путешествий: дубли между трассами, веса, карта покрытия швов из разделов `## Швы`, что уходит в change починки; проверить: ни одной находки без веса, карта покрытия без пустых швов между capability одного путешествия
