# Tasks

Каждый пункт — одна capability, одна dev-задача (issue в Linear Project, один worktree, один PR). Все пункты проверяются одинаково: `openspec validate ajm-2-baseline-specs --strict` проходит, `CONFORMANCE=required tools/test-all.sh` зелёный, в описании PR — источники по каждому требованию. Форма и глубина — `design.md` и пилот `specs/car/rt-link/spec.md`. Пункт 1.1 блокирует все остальные; внутри групп 2–5 задачи независимы. Стенда не требует ни одна.

## 1. Пилот

- [x] 1.1 Сверить пилот `car/rt-link` с кодом и довести до формы образца: каждое требование найдено в коде или тесте, лишнее убрано, пропущенное добавлено; проверить `openspec validate ajm-2-baseline-specs --strict` и `CONFORMANCE=required tools/test-all.sh`; источники: `firmware/car/core/main/rt_link.{c,h}`, `control_proto.{c,h}`, `watchdog.h`, `rt_glue.h`, `test/test_rt_session.c`, `test/test_control_proto.c`, `test/test_rt_glue.c`, `docs/protocol.md` → The real-time channel, `contract/car-api.json` → `rt`, `tools/conformance_rt.py`, `tools/mock_car/rt_link.py` (AJM-3)

## 2. Машинка — `specs/car/`

- [x] 2.1 Спека `car/drive`: clamp → смешивание → план на восемь каналов, trim, параметры колеса, «никогда оба HIGH»; проверить validate и test-all; источники: `firmware/car/core/main/mixer.{c,h}`, `motors.{c,h}`, `car.{c,h}`, `trim.h`, `wheel.{c,h}`, `dims.{c,h}`, `test/test_mixer.c`, `test_motors.c`, `test_trim.c`, `test_wheel.c`, `CLAUDE.md` → Motor channel mapping, `contract/car-api.json` → `config.domains` (`trim`, `wheel`, `chassis`) (AJM-4)
- [x] 2.2 Спека `car/actuator-arbiter`: шесть источников и их ранг, стоп-грант, единственный писатель PCA9685 на 50 Гц, разгон (подъём ограничен, сброс мгновенный), отказ шины и `motors.bus`; проверить validate и test-all; источники: `firmware/car/core/main/link.{c,h}`, `ramp.{c,h}`, `pca9685.{c,h}`, `i2c_bus.{c,h}`, `test/test_link.c`, `test_ramp.c`, `docs/protocol.md` → Telemetry (`motors.owner`, `motors.bus`), `contract/car-api.json` → `groups.motors`, `config.domains.ramp` (AJM-6)
- [x] 2.3 Спека `car/recovery`: хлебные крошки, возврат реверсом при потере связи, отмена по кадру, отмена по `bye`, домен `recovery`; проверить validate и test-all; источники: `firmware/car/core/main/recovery.{c,h}`, `test/test_recovery.c`, `docs/protocol.md` → Command (абзац о срабатывании), `contract/car-api.json` → `config.domains.recovery`, `docs/superpowers/specs/2026-06-14-link-loss-auto-return-design.md` (история) (AJM-7)
- [x] 2.4 Спека `car/calibration`: `/calibration` и `/calibration/spin`, углы по именам, `inverted`, мастер «крутнуть пару — назвать колесо», хранение, влияние на `motors.calibrated`; проверить validate и test-all; источники: `firmware/car/core/main/calibration.{c,h}`, `calib_api.c`, `calib_wire.{c,h}`, `test/test_calibration.c`, `test_calib_wire.c`, `docs/protocol.md` → Calibration, `contract/car-api.json` → `calibration`, `tools/conformance.py` (AJM-8)
- [x] 2.5 Спека `car/config`: `/config` — шесть доменов, GET целиком, POST подмножества с проверкой до применения, диапазоны и умолчания из контракта, по одной JSON-строке на домен в NVS, dirty check; проверить validate и test-all; источники: `firmware/car/core/main/cfg_api.c`, `cfg_json.{c,h}`, `cfg_value.h`, `cfg_contract.h`, `cfg_table.inc`, `test/test_cfg_table.c`, `test_cfg_value.c`, `docs/protocol.md` → Configuration — REST, `contract/car-api.json` → `config`, `tools/conformance.py` (AJM-9)
- [x] 2.6 Спека `car/status-and-version`: `/status` (шесть групп, слова состояния, правила честности), замороженный `/version`, `device` в `hello_ack`, конверт ошибок и коды; проверить validate и test-all; источники: `firmware/car/core/main/status_api.c`, `device_json.{c,h}`, `telemetry.{c,h}`, `api_util.{c,h}`, `test/test_device_json.c`, `test_telemetry.c`, `docs/protocol.md` → `/version`, `/status`, Errors, `contract/car-api.json` → `groups`, `version`, `envelope`, `errors` (AJM-10)
- [x] 2.7 Спека `car/ota-and-rollback`: `POST /ota`, источник `ota` у арбитра, откат загрузчиком до подтверждения, номер сборки и `rolled_back`; проверить validate и test-all; источники: `firmware/car/core/main/ota_api.c`, `main.c` (подтверждение образа), `link.c` (источник `ota`), `docs/protocol.md` → Firmware update, `contract/car-api.json` → `endpoints.ota`, `docs/superpowers/specs/2026-08-23-ota-fix-decisions.md` (история) (AJM-11)
- [x] 2.8 Спека `car/radio-and-network`: C6 как модем, образ `esp_hosted` внутри прошивки, проверка версии и доставка по SDIO при несовпадении, `radio.state`, softAP и личность; проверить validate и test-all; источники: `firmware/car/core/main/wifi_ap.{c,h}`, `radio_expected.{c,h}`, `radio_flash.{c,h}`, `radio_ota.{c,h}`, `identity.h`, `board.h`, `test/test_radio_ota.c`, `firmware/car/modem/README.md`, `contract/car-api.json` → `network`, `groups.radio`, `docs/bringup.md` (наблюдённое, не «Open assumptions») (AJM-12)
- [x] 2.9 Спека `car/video-stream`: сенсор и пайплайн между start/stop, подписка `view` и её владелец, ключевые кадры по расписанию и по `key`, нарезка и отправка струйкой, домен и группа `video`, `GET /snapshot` с `409`/`500`; проверить validate и test-all; источники: `firmware/car/core/main/camera.{c,h}`, `video_enc.{c,h}`, `video_link.{c,h}`, `video_sub.h`, `video_wire.h`, `video_cfg.{c,h}`, `snapshot_api.c`, `frame_crop.h`, `test/test_video_sub.c`, `test_video_wire.c`, `test_frame_crop.c`, `docs/protocol.md` → The video channel, `contract/car-api.json` → `video`, `groups.video`, `config.domains.video`, `tools/conformance_video.py` (AJM-13)

## 3. Адаптер — `specs/dongle/`

- [x] 3.1 Спека `dongle/usb-network`: NCM-интерфейс к телефону, адреса, запрет объявлять себя шлюзом, что видит Mac; проверить validate и test-all; источники: `firmware/dongle/main/usb_net.{c,h}`, `board.h`, `firmware/dongle/README.md` → The board, The dongle must never advertise itself as a gateway, The Mac acceptance run, `contract/dongle-api.json` → `network`, `groups.usb` (AJM-14)
- [x] 3.2 Спека `dongle/car-join`: сеть машинки приходит от приложения при каждом запуске и живёт в RAM, автомат присоединения, uplink, что происходит при перезагрузке и потере AP; проверить validate и test-all; источники: `firmware/dongle/main/wifi_sta.{c,h}`, `wifi_state.{c,h}`, `uplink.{c,h}`, `net_cfg.{c,h}`, `dongle_clock.{c,h}`, `test/test_wifi_state.c`, `test_uplink.c`, `test_net_cfg.c`, `firmware/dongle/README.md` → Bench, `contract/dongle-api.json` → `wifi_request`, `wifi_reply`, `bounds`, `groups.wifi` (AJM-15)
- [x] 3.3 Спека `dongle/relay`: REST `:80` и UDP `4210`/`4211` к машинке байт в байт, таблица сессий, rate gate видео в сторону телефона, счётчики и `video_dropped`; проверить validate и test-all; источники: `firmware/dongle/main/relay_udp.{c,h}`, `relay_tcp.{c,h}`, `tcp_pending.{c,h}`, `udp_sess.{c,h}`, `rate_gate.{c,h}`, `relay_stats.{c,h}`, `test/test_rate_gate.c`, `test_udp_sess.c`, `test_tcp_pending.c`, `test_relay_stats.c`, `firmware/dongle/README.md` → The video relay, `contract/dongle-api.json` → `relay`, `groups.relay` (AJM-16)
- [x] 3.4 Спека `dongle/api`: HTTP `:8080` — `/status` (`usb`, `wifi`, `relay`, `system`), `/version`, `/wifi`, `/ota`, защита, конверт ошибок, запись о сбое с возрастом; проверить validate и test-all; источники: `firmware/dongle/main/net_api.{c,h}`, `status_api.{c,h}`, `status_json.{c,h}`, `ota_api.{c,h}`, `api_guard.{c,h}`, `api_util.{c,h}`, `test/test_status_json.c`, `firmware/dongle/README.md` → API, `contract/dongle-api.json` → `endpoints`, `version`, `envelope`, `errors`, `groups.system` (AJM-17)
- [x] 3.5 Спека `dongle/panel`: пятнадцать экранов одного шаблона, какие выбирает сам и какие листаются, короткое и долгое нажатие BOOT, только собственные измерения; проверить validate и test-all; источники: `firmware/dongle/main/display.{c,h}`, `display_hal.{c,h}`, `screens.{c,h}`, `fonts_cyrillic.{c,h}`, `test/test_screens.c`, `README.md` → The dongle's panel, `docs/superpowers/specs/2026-09-01-dongle-display-design.md` (история) (AJM-18)

## 4. Приложение — `specs/app/`

- [x] 4.1 Спека `app/launch-ladder`: лестница `[адаптер, машинка]`, стадия reach → `/version` → выпуск → правило → шаг, передача сети адаптеру, «другая машинка», стражи после гейта и перезапуск с нужной ступени; проверить validate и test-all; источники: `app/AJMiddleCar/AppFlow.swift`, `StageRule.swift`, `GateRule.swift`, `VersionRule.swift`, `CarReach.swift`, `DongleLink.swift`, `DongleClient.swift`, `CarHost.swift`, `Identity.swift`, `ConnectView.swift`, `app/tests/{stagerule,gate,versionrule,carreach,donglelink,donglestatus,identity}`, `CLAUDE.md` → iOS app, `docs/superpowers/specs/2026-09-17-board-ladder-design.md` (история) (AJM-19)
- [x] 4.2 Спека `app/firmware-update`: получение выпуска, принудительное обновление отставшей платы, порядок плат, прогресс, обрыв и повтор; проверить validate и test-all; источники: `app/AJMiddleCar/FirmwareFlow.swift`, `FirmwareView.swift`, `FirmwareCarView.swift`, `UpdateClient.swift`, `UpdateRules.swift`, `DownloadBar.swift`, `app/tests/update`, `docs/superpowers/specs/2026-09-16-one-release-gate-design.md` (история) (AJM-20)
- [x] 4.3 Спека `app/drive-session`: клиент `car/rt-link` — `hello` до ответа, поток команд, темп, политика сессии, состояние связи, джойстик и геймпад, режимы, экран возврата; проверить validate и test-all; источники: `app/AJMiddleCar/CarLink.swift`, `RTFrame.swift`, `SessionPolicy.swift`, `ControlModel.swift`, `ControlIntent.swift`, `IntentCore.swift`, `PhasePacer.swift`, `LinkState.swift`, `DriveMode.swift`, `Gamepad.swift`, `JoystickView.swift`, `RecoverView.swift`, `CarTransport.swift`, `CarNet.swift`, `app/tests/{carlink,rtframe,sessionpolicy,intent,phasepacer,drivemode}`, `docs/protocol.md` → The real-time channel (AJM-21)
- [x] 4.4 Спека `app/drive-hud`: окно 16:9 в кадре 4:3, приборы по краям, раскладка от экрана и safe area, видео-клиент по правилам приёмника, подписка `view` и запрос ключевого кадра; проверить validate и test-all; источники: `app/AJMiddleCar/DriveView.swift`, `DriveLayout.swift`, `VideoView.swift`, `VideoLink.swift`, `VideoWire.swift`, `SignalBars.swift`, `PowerBar.swift`, `DriveDiagram.swift`, `app/tests/{drivelayout,videowire}`, `docs/protocol.md` → Receiver rules, Subscription, `docs/superpowers/specs/2026-09-15-drive-hud-design.md` (история) (AJM-22)
- [x] 4.5 Спека `app/calibration`: мастер — клиент `/calibration`, экраны углов и направлений, что происходит при ошибке и при уходе с экрана; проверить validate и test-all; источники: `app/AJMiddleCar/CalibrationView.swift`, `CalibClient.swift`, `CarInterface.swift`, `app/tests/carinterface`, `docs/protocol.md` → Calibration (AJM-23)
- [x] 4.6 Спека `app/settings`: шесть доменов `/config` в интерфейсе — группы, экраны, локальное состояние, конфликт с машинкой, пресеты моторов; проверить validate и test-all; источники: `app/AJMiddleCar/SettingsView.swift`, `ConfigStore.swift`, `ConfigState.swift`, `ConfigNotice.swift`, `RampView.swift`, `TrimView.swift`, `VideoSettingsView.swift`, `WheelParamsView.swift`, `CarDimensionsView.swift`, `CarDimsDiagram.swift`, `MotorPresets.swift`, `app/tests/configstate`, `docs/protocol.md` → Configuration — REST (AJM-24)
- [x] 4.7 Спека `app/tricks`: библиотека трюков, редактор геометрии, симуляция траектории, воспроизведение в поток команд и его остановка; проверить validate и test-all; источники: `app/AJMiddleCar/Tricks.swift`, `TrickEditorView.swift`, `TrickSettings.swift`, `TricksSettingsView.swift`, `TrickSim.swift`, `TrickSimView.swift`, `TricksControl.swift`, `docs/superpowers/specs/2026-06-15-tricks-design.md`, `2026-06-17-trick-trajectory-sim-design.md` (история) (AJM-25)
- [x] 4.8 Спека `app/screens-and-theme`: ландшафт, русская локализация, тёплые темы, `SplitScreen` со своим заголовком, галерея экранов и её аргументы запуска, haptics; проверить validate и test-all; источники: `app/AJMiddleCar/AJMiddleCarApp.swift`, `SplitScreen.swift`, `Theme.swift`, `SchemeToggle.swift`, `L.swift`, `GalleryView.swift`, `Haptics.swift`, `DeviceArt.swift`, `CLAUDE.md` → Gotchas 6–7 (AJM-26)

## 5. Сквозное — `specs/shared/`

- [x] 5.1 Спека `shared/contract`: два контракта как источник правды, четыре выражения каждого, что генерируется куда, запрет ручной правки, проверка дрейфа; проверить validate и test-all; источники: `contract/car-api.json`, `contract/dongle-api.json`, `tools/gen_contract.py`, `tools/gen_dongle.py`, `tools/gen_common.py`, `tools/check_contract.sh`, `tools/test_gen_contract.py`, `CLAUDE.md` → The contract (AJM-27)
- [x] 5.2 Спека `shared/release`: один выпуск — приложение и обе прошивки, тег `v<semver>+<build>`, `version.txt`, совместимость «билд платы == тег», `proto` как тег формата, запрет переписывать историю main; проверить validate и test-all; источники: `tools/release.sh`, `version.txt`, `README.md` → Three processors, one release, `CLAUDE.md` → iOS app (абзац о совместимости), `docs/superpowers/specs/2026-09-16-one-release-gate-design.md`, `2026-09-17-proto-out-of-app-design.md` (история) (AJM-28)
- [x] 5.3 Спека `shared/mock-and-conformance`: mock_car с тремя портами, флаги порчи, `MOCK_DEVICE` и `--no-version`, три conformance-прогона, `tools/test-all.sh` как определение зелёного дерева; проверить validate и test-all; источники: `tools/mock_car/mock_car.py`, `rt_link.py`, `video.py`, `state.py`, `README.md`, `tools/conformance.py`, `conformance_rt.py`, `conformance_video.py`, `tools/test-all.sh`, `CLAUDE.md` → Build, iOS app (абзац о моке) (AJM-29)
