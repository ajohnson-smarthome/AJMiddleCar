# Tasks

Пункт — один воркер, один PR. Общая проверка: `openspec validate ajm-178-battery-monitor --strict`; `CONFORMANCE=required tools/test-all.sh`; для аппа — `cd app && xcodegen generate && xcodebuild build -scheme AJMiddleCar -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/ddata-middle`; для прошивки — `source tools/env-p4.sh && cd firmware/car/core && idf.py build`. Поведение — по `specs/car/battery-monitor/spec.md` и дельтам; решения и числа — `design.md`. Порядок: 1.1 и 1.2 параллельно; 2.1, 3.1, 4.1 — после 1.1 (нужны сгенерированные ключи), между собой параллельно. Стендовые пункты — PM.

## 1. Контракт и чистое правило

- [ ] 1.1 Группа `battery` в `contract/car-api.json` (`voltage_mv`, `current_ma`, `power_mw`, `soc_pct` — int nullable; `state` — `swift: BatteryState`, `ok|low|absent`), последней в `telemetry.groups` и `status.groups`; `python3 tools/gen_contract.py` — прошивка, Swift, мок, `docs/protocol.md`; `telemetry.h`: поля `battery_*` в `telemetry_t` (`INT32_MIN` = `null`) и печать группы последней в `telemetry_groups()`; `telemetry_gather` пока заполняет `absent`/`null` (драйвера ещё нет); мок: `values["battery"]` в `status_groups()` константой `absent` (2.1 заменит на модель); проверить: `tools/check_contract.sh` чист, `tools/test_gen_contract.py` и `test_contract_wire` зелёные (вложенность семи групп, `null` в nullable), `CONFORMANCE=required tools/test-all.sh` зелёный, `idf.py build` собирается, `xcodebuild` собирается (`Telemetry.battery` появился в `CarAPI.swift`); источники: `contract/car-api.json` (`groups.link` — образец nullable, `groups.video` — образец state), `tools/gen_common.py:45-75`, `firmware/car/core/main/telemetry.h`, `telemetry.c:121-140`, `firmware/car/core/test/test_contract_wire.c`, `tools/mock_car/state.py:799-817` (AJM-179)
- [x] 1.2 `firmware/car/core/main/battery_pack.h` (константы из `design.md` § 2 — 3S, 3P, 9000 мА·ч, пороги 20/23, покой 500 мА / 30 с, старт 3 с, таблица покоя 12 точек) и чистый `battery_soc.{c,h}` (`init`, `step(mv, ma, dt_ms) → soc или -1`, `rest_pct(mv_per_cell)`, `reset`); хост-тест `firmware/car/core/test/test_battery_soc.c` в `Makefile` рядом с остальными: старт 12,6 В → 100 через 3 с и `-1` до того; старт под током > 500 мА не закрывает окно; 4500 мА·ч отдано от 80 % при 9000 → 30; заряд (`ma < 0`) растит; 30 с покоя тянет к таблице не быстрее 1 %/с и останавливается; интерполяция таблицы между точками; границы 0…100; проверить: `make -C firmware/car/core/test run` зелёный и в `tools/test-all.sh`; источники: `firmware/car/core/main/ramp.{c,h}` + `test/test_ramp.c` (образец чистого модуля с тестом), `firmware/car/core/test/Makefile`, `openspec/changes/ajm-178-battery-monitor/specs/car/battery-monitor/spec.md` (AJM-180)

## 2. Прошивка, мок, апп

- [ ] 2.1 Мок: модель пака в `tools/mock_car/state.py` (`design.md` § 4 — ток от `|throttle|`, напряжение от процента и тока, кулоны при 9000 мА·ч, `low` 20/23, шаг из такта телеметрии), флаги `--battery-soc N` (80), `--battery absent`, `--battery-drain-x N` (1) в `mock_car.py`, раздел флагов в `tools/mock_car/README.md`; `conformance.py` — семь групп `/status` из валидатора, `conformance_rt.py` — `battery.state` из словаря, `soc_pct` в `0…100` или `null`; проверить: `test_state.py` — сценарии «Пак тает под газом» (`--battery-soc 25 --battery-drain-x 600`, газ 1,0 → около 8300 мА, `low` в пределах минуты модельного времени) и «Без монитора» (`absent` → все `null`), `CONFORMANCE=required tools/test-all.sh` зелёный; источники: `tools/mock_car/state.py` (`status_groups`, `telemetry`, флаги деградации — `_bus_ok`, `video_state`), `mock_car.py:455-490` (аргументы), `tools/mock_car/test_state.py`, `tools/conformance.py`, `tools/conformance_rt.py` (AJM-181)
- [ ] 2.2 Прошивка: `board.h` (+`BOARD_INA260_ADDR 0x41`, `BOARD_INA260_HZ 100000`), `power_monitor.h` + `ina260.c` (детект `0xFE/0xFF`, `CONFIG` усреднение 16 × 1,1 мс непрерывно, чтение `0x01/0x02/0x03`, LSB 1,25 мА / 1,25 мВ / 10 мВт, ток со знаком), `battery.{c,h}` (задача приоритет 3, 200 мс, снимок под `portMUX`, три ошибки → `absent`, удача → `ok` + `battery_soc_reset`, гистерезис `low`), `telemetry_gather` берёт снимок, `main.c` — `battery_init()` после `pca9685_init` без влияния на `motors_ok`; `CLAUDE.md` — модули в списке архитектуры; проверить: `idf.py build` собирается, `tools/test-all.sh` зелёный (чистые части покрыты 1.2; сам драйвер — стенд 3.1), `grep -rn 'link_\|motors_' firmware/car/core/main/battery.c firmware/car/core/main/ina260.c` пуст — монитор не знает актуатора; источники: `firmware/car/core/main/i2c_bus.{c,h}`, `pca9685.c` (образец устройства на шине: `i2c_master_bus_add_device`, `scl_speed_hz`), `camera.c` (`camera_init` — отказ не ломает загрузку), `telemetry.c:121-140`, `main.c:196-220`, `docs/research/2026-09-20-ina260-compat-and-wiring.md` § 2–3 (регистры), `docs/bringup.md` (2026-09-20 — живые числа регистров) (AJM-182)
- [ ] 2.3 Апп: `app/AJMiddleCar/BatteryBadge.swift` (иконка 22 × 11 с заливкой по `soc_pct`, «нос», молния при заряде; подпись `L.batteryStats` 10 pt; цвета по `state`: `text`/`warn`/`muted`, «—» при `absent`, пустая иконка при `soc_pct == nil`), в левом `HStack` верхнего ряда `DriveView` после статистики видео; плашка «Батарея разряжена» — четвёртое условие в `hasWarnings`/`warnings`; строки `battery.low`, `battery.stats`, `battery.absent` в `L.swift` и `Localizable.strings`; галерея: `mockLink(battery:)`, кадры «Drive battery low» и «Drive battery absent» сразу после «Drive video off»; проверить: `xcodebuild` собирается, `tools/test-all.sh` зелёный, `grep -n 'tricks\|battery' app/AJMiddleCar/DriveLayout.swift` пуст — раскладка не тронута; источники: `app/AJMiddleCar/DriveView.swift` (верхний `HStack`, `statusItem`, `hasWarnings`, `warnings`), `SignalBars.swift` (образец мини-прибора), `L.swift:160-170`, `GalleryView.swift` (`mockLink`, кадры Drive), `app/AJMiddleCar/Generated/CarAPI.swift` (`Telemetry.battery` после 1.1) (AJM-183)

## 3. Стенд

- [ ] 3.1 (стенд) Прошить main после 2.2: в логе загрузки — детект монитора; `/status.battery` через адаптер — `voltage_mv` против мультиметра на клеммах (±50 мВ), `current_ma` при `mix 0.3 0` и `mix 1 0` против токовых клещей или лампы 12 В 21 Вт (≈1750 мА); `soc_pct` появляется через 3 с; `link.rx_hz` = 10 с монитором на шине; результат — `docs/bringup.md`
- [ ] 3.2 (стенд) Таблица покоя по HG2: напряжение покоя пака после ≥ 10 мин отдыха на четырёх уровнях (полный; после ~2 А·ч; ~4,5 А·ч; ~7 А·ч отданных по `soc_pct`) — поправить точки в `battery_pack.h`, если расходятся с типовой таблицей больше чем на 5 %; `docs/bringup.md`
