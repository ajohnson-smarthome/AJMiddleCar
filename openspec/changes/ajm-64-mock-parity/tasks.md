# Tasks

Пункт — один воркер, один PR; issues в скобках — что закрывает (`Closes` в PR). Общая проверка: тест, падающий до починки (в `tools/mock_car/test_*.py` или `tools/test_gen_contract.py`); поведение после — по дельте `specs/shared/mock-and-conformance/spec.md`; `openspec validate ajm-64-mock-parity --strict`; `CONFORMANCE=required tools/test-all.sh`. Исходные issues — источник `file:line` и «направления починки»; трасса J8 — таблица по каждой спеке `car/*`. Стенда не требует ни один пункт. 1.1 → 1.2 → 1.4 идут последовательно (одни файлы), остальное параллельно.

## 1. Мок

- [ ] 1.1 Перезагрузка мока глушит и REST на время паузы; `build` — цифры после первого `+` до первого не-цифрового (AJM-103, AJM-108); проверить `test_state.py`: `v1.0+784-dirty → 784`, `v1.0+9000-dirty-with-a-long-branch-name → 9000`; тест HTTP: в паузе соединение не принимается / рвётся до ответа, после — `/version` с новой версией; `--rollback` доводит до прежней версии после паузы; источники: `tools/mock_car/mock_car.py:250-300`, `state.py` (`build_number`, `simulate_reboot`), `rt_link.py` (`REBOOT_QUIET_S`), `firmware/car/core/main/device_json.c` (`fw_build_number`)
- [ ] 1.2 `/ota` мока проверяет `chip_id` и магическое слово дескриптора после чтения тела → `400 not_firmware`; лимиты тела REST (три числа с комментарием на источник) и `bad_json` без `field` на длинное тело и на тело-не-объект в `/calibration` (AJM-105, AJM-113); проверить тестами мока: образ адаптера (`chip_id` S3) → `not_firmware`, версия прежняя; тело в 512 байт → `bad_json`; массив в `/calibration` → `bad_json`; источники: `tools/mock_car/mock_car.py:255-275`, `state.py:118-140`, `firmware/car/core/main/ota_api.c`, `cfg_api.c` (лимиты), `calib_api.c`
- [ ] 1.3 Эмиттер валидатора `/config` в `tools/gen_contract.py`: `math.isfinite` до арифметики → `wrong_type`; `int`/`enum` приводятся к `int` после проверки, `1e2` хранится и печатается целым (AJM-111); проверить `tools/test_gen_contract.py` тремя телами (`1e400`, `NaN`, `1e2`) и `python3 tools/gen_contract.py && tools/check_contract.sh`; источники: `tools/gen_contract.py` (эмиттер Python), `tools/mock_car/generated.py` (порождённый — не править руками)
- [ ] 1.4 Флаги деградации (после 1.1 и 1.2): `--bus down`, `--camera off`, `--radio mismatch|unavailable`, `--reset-at-boot`, `--write-fail <domain|calibration>`, `--reboot-s N`; каждый отражается во всех местах, где машинка это показывает, все выключены по умолчанию (AJM-116); проверить тестом на каждый флаг в `test_state.py`/тесте мока: `--bus down` → `motors.bus: down` в телеметрии и `/status`, `spin` → `409`, крошек нет; `--reset-at-boot` → `true` и умолчания; `--write-fail ramp` → один `500`, домен откачен; `README.md` мока — раздел про флаги; источники: `tools/mock_car/mock_car.py` (аргументы, по образцу `--rollback`/`--rssi`), `state.py`, `video.py`, `openspec/specs/car/status-and-version/spec.md`, `car/video-stream/spec.md`, `car/calibration/spec.md` (что показывает машинка)
- [ ] 1.5 Клип мока 1280×720 (`ffmpeg testsrc2=size=1280x720`, те же кодек/длительность, ≤ 400 КБ), README и docstring `video.py`; `test_video.py` разбирает SPS и закрепляет размеры против `VIDEO["width"]`/`VIDEO["height"]` (AJM-110); проверить `python3 tools/mock_car/test_video.py` и `tools/conformance_video.py` против мока; источники: `tools/mock_car/sample.h264`, `video.py`, `README.md`, `contract/car-api.json` → `video`
- [ ] 1.6 Тест зеркал `tools/mock_car/test_mirrors.py` в `test-all.sh`: константы прошивки без ключа в контракте (порог неподвижности, предел сегмента возврата, длительность импульса, такт, длина sid, срок ожидания OTA) читаются из исходников регулярными выражениями и сравниваются с копиями мока и `SessionPolicy`/`stallTimeout` пульта (AJM-121); проверить: намеренно изменённая константа в моке роняет тест с именем и обоими значениями; источники: `tools/test_gen_contract.py` (`test_ranges_match_the_firmware_today` — образец), `tools/mock_car/state.py`, `rt_link.py`, `firmware/car/core/main/recovery.c`, `link.h`, `calib_api.c`, `rt_link.h`, `app/AJMiddleCar/SessionPolicy.swift`

## 2. Conformance

- [ ] 2.1 RT-прогон: легенды сторож (нули → 0,5 с тишины → `link.timeouts` +1, `motors.owner: idle`, сессия жива без `hello`), возврат (1 с `throttle: 0.3` → тишина → `recovering` в первом кадре), повтор `hello` не сбрасывает gate, простой сессии — только с `--slow` (AJM-117); проверить против мока (`CONFORMANCE=required tools/test-all.sh`) и что каждая легенда безопасна для машинки на подставке (без езды дольше секунды); дельта `MODIFIED` «RT-прогон…» в `specs/shared/mock-and-conformance/spec.md`; источники: `tools/conformance_rt.py`, `openspec/specs/car/rt-link/spec.md`, `car/recovery/spec.md`
- [ ] 2.2 Видео-прогон: чужой sid и `hello` на видеопорту → тишина 1 с; `/status.video` посреди потока — `streaming`, `fps > 0`; `bye` → ни одной датаграммы через 0,5 с и `video.state: idle`; вытеснение вторым сокетом → поток кончается; гистограмма интервалов чанков ключевого кадра — медиана ≈ 3 мс (AJM-118); проверить против мока (после 1.4 — и с `--camera off`: `view` игнорируются); дельта `MODIFIED` «Видео-прогон…»; источники: `tools/conformance_video.py`, `openspec/specs/car/video-stream/spec.md`
- [ ] 2.3 REST-прогон: домен не объект, `proto` в теле → `unknown_field`, формы записей `wheels[i]`, углы не по порядку, второй `spin` и `reverse`, `rx_hz` без потока — два опроса подряд `0` (AJM-119); проверить против мока; дельта `MODIFIED` «REST-прогон…»; источники: `tools/conformance.py`, `openspec/specs/car/config/spec.md`, `car/calibration/spec.md`, `car/status-and-version/spec.md`
- [ ] 2.4 Flaky `Connection refused` от HTTP мока при живом видео (AJM-86): `test-all.sh` сохраняет `$LOG` мока при ненулевом выходе conformance и печатает путь; воспроизвести циклом 20 × `conformance_video.py` против мока; воспроизвёлся — починить (event loop мока при старте потока), нет — комментарий в issue «20 прогонов, не воспроизвелось, лог сохраняется» и статус Canceled; проверить: сломанный намеренно прогон оставляет лог; источники: `tools/test-all.sh` (блок conformance), `tools/mock_car/mock_car.py` (HTTP-сервер и запуск видеопотока), `video.py`
