# Spec Delta

## MODIFIED Requirements

### Requirement: Стражи после гейта — перезапуск с нужной ступени, без своих экранов
Когда лестница пройдена, приложение SHALL открыть сессию (`app/drive-session`) и держать экран поиска, пока машинка не ответит `hello` и телеметрия не пойдёт. `fw` из `hello` SHALL сверяться с тегом выпуска при каждом `hello`, меняющем `fw`, и при каждой передаче управления: `build` не ниже тега — экран вождения; младше, без номера или при неизвестном теге — лестница перезапускается со ступени машинки, где правило по `/version` поднимет «обновление»; пока `hello` не пришёл, сверять нечего. «Провод пропал» и «локальная сеть запрещена» SHALL перезапускать лестницу со ступени адаптера — своих экранов у них нет; в лестнице без адаптера этот страж не действует. **Затянувшийся поиск** SHALL быть третьим стражем: если после гейта сессия не открывается дольше порога (порядка бюджета присоединения адаптера), приложение SHALL перечитать `/status` адаптера, и при станции в `failed` или `idle` — или без сети машинки вовсе (`wifi.ssid` не её) — перезапустить лестницу со ступени машинки, где стадия сама передаст сеть заново и покажет «не удалось подключиться / Повторить», если и это не поможет; станция в `searching`/`joining`/`connected` — ждать дальше, и `/status` перечитывается не чаще раза в порог; тишина, ошибка или запрет на самом `/status` — тоже ждать: провод — дело стража адаптера. Без адаптера в лестнице третьего стража нет — спросить некого. То же правило SHALL действовать, пока машинкой владеет экран прошивки в ожидании её ответа, — с тем отличием, что парковка стадии сохраняется: страж посылает адаптеру `POST /wifi` сам, не чаще раза в порог, шага не меняет и машинку не опрашивает. Перезапуск SHALL ставить лестницу на ступень названной платы или раньше; второй раннер SHALL NOT создаваться. Открытую сессию перезапуск SHALL NOT закрывать.

#### Scenario: Провод выдернут во время езды
- **WHEN** сессия жива и USB-интерфейс адаптера пропадает
- **THEN** приложение уходит на стадию адаптера — «нет адаптера», — а не рисует отдельный экран потери провода

#### Scenario: Машинка перезагрузилась в старую сборку
- **WHEN** после передачи управления приходит `hello` с `fw` младше тега
- **THEN** лестница перезапускается со стадии машинки и по `/version` показывает «обновление»

#### Scenario: Живая сессия, лестница только что пройдена
- **WHEN** сессия уже жива в момент передачи управления
- **THEN** `fw` сверяется сразу, без ожидания следующего `hello`; экран вождения открывается, если сборка текущая

#### Scenario: Машинку выключили и включили
- **WHEN** сессия жива, машинка выключена на минуту и включена снова, адаптер за это время исчерпал бюджет присоединения
- **THEN** приложение замечает `failed` у адаптера, перезапускает лестницу со ступени машинки, адаптер получает сеть заново, и езда возобновляется без перезапуска приложения

#### Scenario: Машинка не вернулась под экраном прошивки
- **WHEN** экран прошивки ждёт ответа машинки, а станция адаптера ушла в `failed`
- **THEN** сеть передаётся адаптеру заново тем же стражем, ожидание продолжается; «ждать вечно» нет

### Requirement: Выпуск узнаётся один раз, до правила; без него — удержание
Выпуск (`shared/release`) SHALL запрашиваться, когда ответившая плата ждёт правила, а тег ещё не известен, и не чаще одного удачного раза за запуск: узнанный тег SHALL служить обеим платам. Пока тег запрашивается, SHALL показываться «проверяю обновления». Тег SHALL приниматься, только если несёт номер сборки и выпуск несёт образы обеих плат — адаптера и машинки, независимо от того, чья стадия его запросила; иначе — удержание: «нет интернета», когда лента выпусков недоступна, «нет выпуска для платы» с тегом и той платой, чьего образа нет (или без платы, когда нет номера сборки), когда выпуск прочитан, но неполон. Ответ ленты `403` или `429` SHALL NOT читаться как «нет интернета»: это «лента отказала» — лента ограничила запросы с этого адреса, — своё удержание с ожиданием, которое назвала лента (`retry-after`, иначе `x-ratelimit-reset`, иначе минута), не короче нижней границы темпа и не дольше часа. Удержание SHALL NOT нести кнопки и SHALL сниматься само: плата опрашивается в темпе лестницы, а лента — **в своём темпе**, реже плат: после первого неудачного чтения — через 20 с, дальше с нарастанием до 60 с, пока удержание стоит; после отказа — по названному лентой ожиданию; первый удачный ответ пускает стадию к правилу, и темп начинается заново. «Повторить» на шаге «откат» SHALL спрашивать ленту сразу, минуя темп. Между чтениями ленты удержание SHALL оставаться на экране — плата, замолчавшая и вернувшаяся посреди удержания, не оставляет на экране свой «тихий» шаг. Пока удержание стоит, «проверяю обновления» SHALL NOT показываться заново на каждом опросе. Кэш образа на телефоне SHALL NOT заменять знание о выпуске (`shared/release`).

#### Scenario: Лента выпусков недоступна
- **WHEN** адаптер ответил, а последний выпуск прочитать не удалось
- **THEN** показывается «нет интернета» без кнопки; когда интернет появляется, стадия продолжается сама

#### Scenario: Выпуск без образа адаптера или без номера сборки
- **WHEN** последний выпуск не несёт образа адаптера или помечен тегом без `+<build>`
- **THEN** тег не принимается: показывается «нет выпуска» с тегом этого выпуска, и адаптер не считается текущим

#### Scenario: Выпуск без образа машинки на стадии адаптера
- **WHEN** тег запрашивается на стадии адаптера, образ адаптера в выпуске есть, а образа машинки нет
- **THEN** тег не принимается: показывается «нет выпуска» с тегом и платой «машинка», лестница не идёт дальше стадии адаптера

#### Scenario: Вторая плата
- **WHEN** тег узнан на стадии адаптера, и машинка ответила
- **THEN** выпуск не запрашивается снова: правило машинки применяется с тем же тегом, и образ машинки в нём заведомо есть

#### Scenario: Долгое удержание
- **WHEN** «нет выпуска для машинки» стоит три минуты
- **THEN** лента спрошена считанные разы (не больше шести), экран остаётся «нет выпуска», и адрес телефона не заперт лентой

#### Scenario: Лента ограничила запросы
- **WHEN** лента отвечает `403` с `x-ratelimit-reset` через полчаса
- **THEN** показывается «лента отказала» с ожиданием около получаса, не «нет интернета»; следующее чтение ленты — по истечении ожидания, платы опрашиваются как прежде

#### Scenario: Плата отошла посреди удержания
- **WHEN** стоит «нет интернета», адаптер на секунду замолчал на `/version` и снова ответил, а срок следующего чтения ленты не наступил
- **THEN** на экране снова «нет интернета», а не «нет адаптера»; лента до срока не спрашивается

### Requirement: Откат — сообщение, «Повторить», одна проверка за нажатие
Плата с `rolled_back: true` SHALL показываться шагом «откат» с единственной кнопкой «Повторить»; способа поехать на откатившемся образе SHALL NOT быть (`shared/release`). Точка отсчёта, от которой выпуск считается «новее», — **последняя сборка, залитая в эту плату с телефона**: приложение SHALL запоминать тег образа по плате в момент `ok` заливки (`app/firmware-update`) — именно тот образ, что откатился и никогда не доходит до «Готово», — отдельно для машинки и адаптера. С известной точкой отсчёта правило SHALL решать с первого же взгляда, без нажатия: тег новее залитой сборки и новее `fw` платы — шаг «обновление»; иначе — «откат». Без записи — телефон эту плату не заливал — точкой отсчёта SHALL служить тег, что был на руках при нажатии «Повторить», а до нажатия — «откат». Нажатие SHALL забывать узнанный тег и запрашивать выпуск заново — сразу, не по следующему опросу — и SHALL давать правилу ровно один взгляд от той же точки отсчёта; взгляд SHALL тратиться первым же решением, каким бы оно ни было: следующее нажатие — следующий взгляд. Мерить «новее» от тега на руках при известной записи SHALL NOT: в любом запуске после отката лестница узнаёт новейший тег раньше экрана «откат», и такая мера сравнивала бы выпуск с самим собой. Запись может отстать от платы, прошитой кабелем; лишнее «обновление» от этого безвредно — та же сборка заливается не больше одного раза, потому что заливка обновляет запись, — а пропуск невозможен: сверка с `fw` платы остаётся.

#### Scenario: Ничего новее нет
- **WHEN** нажата «Повторить», а последний выпуск — тот же, что был залит
- **THEN** снова «откат»: тот же образ в тот же откат не заливается

#### Scenario: Вышел новый выпуск
- **WHEN** нажата «Повторить», и тег новее залитой сборки и новее `fw` платы
- **THEN** шаг «обновление» — плата уходит на принудительное обновление

#### Scenario: Плата уже на новом
- **WHEN** тег новее залитой сборки, но `fw` платы не ниже него
- **THEN** снова «откат»: заливать нечего

#### Scenario: Чинящий выпуск в следующем запуске
- **WHEN** выпуск 890 залит с телефона и откатился, опубликован 900, приложение перезапущено, и лестница узнала 900 до того, как дошла до платы с `fw` 880 и `rolled_back: true`
- **THEN** шаг «обновление» с первого взгляда — без «откат» и без нажатия; плата получает 900

#### Scenario: Телефон эту плату не заливал
- **WHEN** записи о залитой сборке нет, плата отвечает `rolled_back: true`, и нажата «Повторить»
- **THEN** точка отсчёта — тег, что был на руках при нажатии: новее него и `fw` платы — «обновление», иначе — «откат»

### Requirement: Один запрос на вход — потом «Повторить»
На один вход в стадию машинки приложение SHALL посылать адаптеру не больше одного **принятого** `POST /wifi` — первичного или повторного: бюджет попыток живёт на стороне адаптера (`dongle/car-join`), приложение просит один раз, адаптер тратит свой бюджет, и дальше оба ждут человека. Принятый запрос — тот, после которого `/status` адаптера несёт сеть машинки; `POST /wifi`, после которого `/status` по-прежнему без сети машинки (запрос потерян — провод, перечисление адаптера в тот же миг), SHALL NOT считаться потраченным: адаптеру нечего было пробовать, и стадия SHALL показывать «передаю сеть» и посылать сеть снова на следующем опросе, без нажатия, пока адаптер её не примет, — не «не удалось подключиться» о присоединении, которого не было. Когда принятый запрос уже потрачен, а `/status` с сетью машинки снова требует запроса (`failed`, `idle`), стадия SHALL держать «не удалось подключиться» с единственной кнопкой «Повторить»; нажатие даёт новый запрос и опрашивает сразу. `connected` SHALL возвращать запрос; вход в стадию машинки заново — после отката на ступень адаптера или после обновления — SHALL начинаться с нового запроса.

#### Scenario: Сеть не поднялась
- **WHEN** после переданной сети `/status` снова просит передать её или повторить
- **THEN** показывается «не удалось подключиться» с «Повторить»; второй `POST /wifi` без нажатия не уходит

#### Scenario: «Повторить»
- **WHEN** нажата «Повторить» на «не удалось подключиться»
- **THEN** следующий опрос посылает `POST /wifi` снова

#### Scenario: Возврат к адаптеру и обратно
- **WHEN** стадия машинки ушла на ступень адаптера и вернулась
- **THEN** запрос снова доступен без нажатия

#### Scenario: `POST /wifi` потерян
- **WHEN** сеть передана, а следующий `/status` несёт пустой `wifi.ssid` — адаптер запроса не получил
- **THEN** показывается «передаю сеть», `POST /wifi` уходит снова без нажатия; «не удалось подключиться» появляется только после `failed` с сетью машинки в `/status`
