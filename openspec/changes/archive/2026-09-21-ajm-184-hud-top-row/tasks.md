# Tasks

Пункт — один воркер, один PR. Общая проверка каждого: `openspec validate ajm-184-hud-top-row --strict`; `CONFORMANCE=required tools/test-all.sh`; `cd app && xcodegen generate && xcodebuild build -scheme AJMiddleCar -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/ddata-middle`. Поведение — по дельте `specs/app/drive-hud/spec.md`, решения и числа — `design.md`, эталон — канвас «HUD: верхний ряд» (ссылка в `design.md`). 1.1 → 2.1 последовательно: оба трогают `DriveView.swift`. Симулятор и снимки — у PM (3.1).

## 1. Лента и пилюли

- [x] 1.1 Лента вместо верхнего градиента и две пилюли одной высоты: в `DriveView` `scrim(.top)` заменить на ленту `HudBand.height = 56` цвета `bg` с линией `line` 1 pt по нижней кромке, те же модификаторы, что у скрима (`ignoresSafeArea`, `allowsHitTesting(false)`), рисуется только при `windowLive`; `scrim(.bottom)` оставить; `notices.padding(.top, HudBand.height + 8)`; `enum HudPill { height 32, radius 10 }` в `ControlBar.swift`, бар и `SchemeToggle` берут высоту и скругление из него (сегменты переключателя — `frame(height:)` вместо `padding(.vertical, 6)`, r10 вместо r9), тела пилюль не меняются; `TricksControl.tint` — `warn` только при `isRunning`, иначе `text`; `CLAUDE.md`, абзац «The drive screen is one layout…» — лента и пилюли (левую группу допишет 2.1); док-комментарии `DriveView`/`SchemeToggle`/`TricksControl` без упоминаний градиента сверху и акцента у искорки; проверить: `tools/test-all.sh` зелёный, `xcodebuild` собирается, `grep -n 'scrim(.top)\|cornerRadius: 9' app/AJMiddleCar/DriveView.swift app/AJMiddleCar/SchemeToggle.swift` пуст, `grep -n 'p.accent' app/AJMiddleCar/TricksControl.swift` не находит цвет глифа сегмента; источники: `app/AJMiddleCar/DriveView.swift` (`scrim`, `notices`, верхний `HStack`), `SchemeToggle.swift`, `ControlBar.swift`, `TricksControl.swift:43`, `Theme.swift`, `openspec/specs/app/drive-hud/spec.md` (AJM-185)

## 2. Левая группа

- [x] 2.1 Левая группа одним правилом и прибор картинки (после 1.1): новый `app/AJMiddleCar/HudItem.swift` — `HudItem(glyph:caption:tint:)` (глиф в боксе 14 pt, зазор 6, подпись 11 pt `monospacedDigit`, цвет прибора целиком) и `HudDivider` (1 × 14 pt, `line`); ряд слева в `DriveView` — `HStack(spacing: 14)`: связь · разделитель · [картинка · разделитель, только при `hasPicture`] · батарея, `statusItem` удалить; `BatteryBadge` — на `HudItem`, `BatteryGauge` и тест `batterygauge` не трогать; новый чистый `VideoGauge.swift` (`make(fps:lost:)` → `caption` «N к/с», `lost: Int?` — `nil` при 0, `accessibility` «N к/с, потеряно M») с хост-тестом `app/tests/videogauge/{main.swift,sources}` (0 потерь → `lost == nil`, 7 → `7`, подписи по спеке), новый `VideoBadge.swift` — `HudItem` с SF `video` 14 pt + при `lost` пара `rectangle.dashed` 12 pt · число 11 pt в `warn` с зазором 10, `accessibilityLabel` из `VideoGauge`; строки: `video.stats` → `"%d к/с"`, новая `video.lost` = `"потеряно %d"` (`L.videoStats(fps:)`, `L.videoLost(_:)`); `VideoLink` — `#if DEBUG static func preview(fps:lost:hasPicture:)`, `CarLink.preview(…, video:)`; галерея: кадры вождения получают `video: .preview(fps: 25, lost: 0, hasPicture: true)`, новый кадр «Drive picture lost» (`fps: 22, lost: 7`) после «Drive battery absent»; `CLAUDE.md` — абзац про экран вождения: `HudItem`, «N к/с» и потери при M > 0; проверить: `tools/test-all.sh` зелёный (в том числе `videogauge`), `xcodebuild` собирается, `grep -rn 'statusItem\|потеряно %d' app/AJMiddleCar/DriveView.swift` пуст, `grep -n 'к/с' app/AJMiddleCar/Resources/ru.lproj/Localizable.strings` показывает `"%d к/с"` без слова «потеряно»; источники: `app/AJMiddleCar/DriveView.swift` (верхний `HStack`, `statusItem`), `BatteryBadge.swift`, `BatteryGauge.swift` (образец чистого вердикта), `app/tests/batterygauge/` (образец теста и `sources`), `SignalBars.swift`, `VideoLink.swift:17-20`, `CarLink.swift:299`, `GalleryView.swift:158-171`, `L.swift:168-176`, `Resources/ru.lproj/Localizable.strings:153-162` (AJM-186)

## 3. Проверка глазами

- [x] 3.1 (PM) Снимки галереи в симуляторе после слияния 2.1, светлая и тёмная тема (`xcrun simctl ui booted appearance light|dark`, `-gallery -galleryIndex N`, `sips -r -90`): «Drive arcade», «Drive tricks open», «Drive video off», «Drive battery low», «Drive battery absent», «Drive picture lost» (индексы — по списку в `GalleryView`, они сдвинулись на один после «Drive battery absent»), плюс живой экран против мока с видео (`-viaDongle` не нужен, мок на 127.0.0.1) — лента над настоящим кадром; сверить с канвасом: пилюли одной высоты и одного радиуса, ✦ цветом текста в покое, три прибора слева одним кеглем с разделителями, «22 к/с» и «⌗ 7» в `warn`, батарея сдвигается к связи без картинки, предупреждение под линией ленты; 11 pt читается; расхождения — issues в AJM-184, не правки в main
