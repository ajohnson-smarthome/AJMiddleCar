# Стендовый список — что решает только плата

Сведено из разделов `## Стенд` восьми трасс (3.1), стендовых пунктов `ajm-64-known-fixes` (5.1, 5.2)
и трёх проверок из `docs/IDEAS.md`, которые не делались никогда. Каждая строка — что сделать,
что ожидается, какие issues закрывает или взвешивает. Результаты — в `docs/bringup.md`, как всегда.

Стенд: `-viaDongle` с адаптером на Mac или телефон; консоли машинки и адаптера пишут в файл.
Порядок ниже — по цене ошибки: сначала то, что подтверждает или снимает `high`.

## 0. Первыми — то, что взвешивает high

| # | Что сделать | Ожидаемый результат | Закрывает |
|---|---|---|---|
| 0.1 | Осциллограф на выходе PCA9685 (или входе BTS7960), `mix 0.5 0` с консоли, RESET на P4 на ходу; измерить от сброса до первого нуля на выходе | Сегодня ШИМ идёт сквозь сброс до `pca9685_zero_all()` в `app_main`; если окно ≥ сотен мс — ответ в проводке `~OE`, не в коде | AJM-128, вес AJM-127 |
| 0.2 | Удерживая SDA на земле, нажать RESET; отпустить SDA через ~5 с | Либо драйвер IDF чистит шину сам и через ≤ 2 с `PCA9685 boards are up` — AJM-127 понижается; либо `init` падает по таймаутам до батареи — AJM-127 подтверждён как urgent | AJM-127 |
| 0.3 | Обновление машинки через приложение, обе консоли пишут: от `ok` на `/ota` до «Готово»; приходит ли адаптеру `WIFI_EVENT_STA_DISCONNECTED` при перезапуске точки; сколько страйков uplink и когда `rejoin` | По коду ~55–58 с, «Готово» на грани; если станция получает disconnect сама — AJM-124 сужается, но не исчезает | AJM-124, AJM-96 |
| 0.4 | При `connected` выключить машинку; засечь по панели («Попытка N из 5») длительность попытки и момент «Нет сети»; включить машинку через 30 с | По спеке `dongle/car-join` адаптер вернётся сам; по коду — `failed` через ~10 с и ничего: тогда сценарий спеки ложен, а AJM-95 достигается выключением-включением | AJM-95, спека car-join |
| 0.5 | Геймпад под обязательным мастером и под листом настроек (живой iOS с контроллером): отклонить стик при открытом листе | Сегодня машинка едет; после починки — стоит | AJM-101 |
| 0.6 | Две руки: стик удержан, второй рукой тап по шестерёнке | Под листом команда — ноль, ручка после закрытия в центре; если колёса крутятся — SwiftUI не вызвал `onEnded`, и починка AJM-101 должна обнулять намерение при накрытии | AJM-101 (`?` J2) |
| 0.7 | Реальный откат (образ с паникой до `mark_app_valid`, из `docs/IDEAS.md` п. 4): OTA → откат → «откат» → «Повторить» | Сегодня: «Требуется обновление» без кнопок. После починки: повторная заливка с предупреждением | AJM-123 |
| 0.8 | После отката опубликовать чинящий выпуск, перезапустить приложение, «Повторить» на «откатилось» | Сегодня: снова «откатилось» — кабель. После починки: «обновление» и прошивка чинящего выпуска | AJM-132 |
| 0.9 | Удержание «нет выпуска для машинки» (снять `ajmiddlecar.bin` с последнего релиза) три минуты; `curl api.github.com/rate_limit` с того же адреса | Сегодня: через ~2 минуты «Нет интернета», `remaining: 0`. После починки: «нет выпуска» всё время | AJM-136 |
| 0.10 | Платы PCA9685 включить после загрузки машинки (`motors.bus: down` в логе), стик не трогать | Сегодня `bus` остаётся `down` и HUD пугает; после починки — `ok` без команды | AJM-91 |

## 1. Известные починки — подтверждение на плате (`ajm-64-known-fixes` 5.1, 5.2)

| # | Что сделать | Ожидаемый результат | Закрывает |
|---|---|---|---|
| 1.1 | `tools/conformance.py http://192.168.7.1` через адаптер; отдельно `curl -X POST --data-binary @<4 КБ нулей> …/ota` | Шаг с не-образом → `400 not_firmware`; `/version` прежний, `rolled_back:false`, `motors.owner` снова `idle` | known-fixes 5.1 (AJM-37/65) |
| 1.2 | Два пульта, оба на экране вождения с видео: второй забирает сессию | Картинка у первого гаснет в пределах такта (`viewer gone`/`owner changed` в логе), у второго — с ключевого кадра после его первого `view`; `video.state`/`fps` в `/status` согласны | known-fixes 5.2 (AJM-40/66), AJM-118 |
| 1.3 | `POST /ota` образом адаптера (`ajdongle.bin`) на машинку | `400 not_firmware` после полной загрузки (`esp_ota_end`), работающий образ цел | AJM-105 (сторона машинки) |
| 1.4 | `tools/conformance.py` на машинке со стендовой сборкой (`fw` с `-N-gSHA` или `-dirty`) | Сегодня `FAIL /version.build`; после AJM-108 — зелёный | AJM-108 |
| 1.5 | `curl -X POST --data-binary @<4 КБ нулей> 192.168.7.1:8080/ota` на адаптер | Сегодня «connection reset» без тела; после починки — `400 not_firmware` конвертом | AJM-137 |

## 2. Из `docs/IDEAS.md` — никогда не проверялось

| # | Что сделать | Ожидаемый результат | Закрывает |
|---|---|---|---|
| 2.1 | Сторож на ходу (п. 5): мгновенное выключение телефона на ходу | 300 мс → задний ход по крошкам → стоп; `link.timeouts` = 1; через 10 с `motors.owner: idle` | J3 С7, `docs/bringup.md` → Watchdog |
| 2.2 | Апп на реальном iPhone (п. 6): первый запуск без разрешения локальной сети; «Не разрешать» → «нет доступа» с «Открыть настройки»; разрешение → шаг снимается сам | Промпт — короткое «ищу», не «отвечает с ошибкой»; после разрешения лестница идёт без перезапуска | J1 шаг 13 |
| 2.3 | Реальный OTA с откатом на битом образе (п. 4) — строки 0.7 и 0.8 выше | — | AJM-123, AJM-132 |


## J1 Первый запуск — как записано в `01-first-launch.md`

| Проверка | Ожидаемый результат | Закрывает |
|---|---|---|
| Обновление машинки через приложение (`-viaDongle`), обе консоли пишут: от `ok` на `/ota` до «Готово»; приходит ли адаптеру `WIFI_EVENT_STA_DISCONNECTED` при перезапуске точки; сколько страйков и когда `rejoin` | «Готово» внутри 60 с, без «Прошито»; если станция не получает disconnect — переприсоединение по страйкам раньше окна | F2 (AJM-124), F1 (AJM-123) |
| Первая загрузка образа выпуска на машинке с нарочно откаченным радио (`firmware/car/modem/README.md`): время передачи 1,15 МБ по SDIO, вторая загрузка, `radio.state: ok`, `rolled_back: false`; общее время от `ok` до `/version` новой сборки | Ворота срабатывают один раз, счётчик обнулён, вся двойная загрузка укладывается в 60 с сторожа | шаг 9, F2 |
| Пять попыток адаптера при выключенной машинке: время от `POST /wifi` до `wifi.state: failed` (`attempts.used` по `/status`); время от подачи питания на машинку до первого ответа `/version` (холодная загрузка, с камерой) | Оба числа записаны в `bringup.md`; машинка, включённая одновременно с адаптером, укладывается в бюджет | шаг 6, F4 (AJM-125) |
| Машинка выключена на 20+ с во время `.stage(.car, .updating)` в фазе «Жду, пока устройство выйдет на связь», потом включена | Сегодня: адаптер `failed`, экран ждёт вечно; после починки — переприсоединение и заливка | F4 (AJM-125) |
| Первый запуск приложения на телефоне без ранее выданного разрешения локальной сети: что показывает лестница, пока открыт системный промпт; «Не разрешать» → «нет доступа» с «Открыть настройки»; разрешение в настройках → шаг снимается сам | Промпт — короткое «нет адаптера»/«ищу», не «отвечает с ошибкой»; после разрешения лестница идёт без перезапуска | шаг 13 |
| День-флаг без кабеля (обе платы `404`) → адаптер `.updating` → машинка `.updating` → S28 — `bringup.md:108-109` | Как записано в bringup | шаги 4–8 |
| Адаптер выдернут посреди `.stage(.car, .updating)` → `.stage(.dongle, .absent)` → воткнут → `.searching/.joining` → снова `.updating` → S28 — `bringup.md:110-111` | Как записано в bringup; заливка, оборванная на середине, отвергнута машинкой (`internal`), актуатор отпущен | шаг 5, 8 |
| После обновления машинки лестница показывает `.searching/.joining` до `.checking` — `bringup.md:112` | По коду `reached` не сбрасывается и `/status` не читается, пока `/version` отвечает: «ищу/подключаю» появятся только на тишине | шаг 5, 8 |
| Первое включение с обесточенными PCA9685 и чистым NVS: экран вождения, мастер, «Крутить» | Сегодня: `200`, вопрос о колесе, выхода нет — строка `AJM-93` | F3 (AJM-93) |


## J2 Поездка — как записано в `02-drive.md`

Пункты видео-раздела `docs/bringup.md` («Leaving the drive screen», «The video switch», «Stream
through the wire») закрывают шаги 5, 6, 11–12 в общем виде; ниже — то, что решает только плата
или живой iOS, с ожидаемым результатом.

- **Две руки: стик удержан, второй рукой тап по шестерёнке.** Ожидание: под листом настроек
  команда — ноль (`link.rx_hz` 10, `motors.owner` `remote`, колёса стоят), ручка стика после
  закрытия листа в центре. Если колёса крутятся под листом — SwiftUI не вызвал `onEnded` при
  отмене жеста, и починка находки 1 должна обнулять намерение при накрытии, а не полагаться
  на жест. (Шаг 8, `?`.)
- **Холодный запуск через адаптер с включённым видео.** Ожидание по коду: экран вождения на
  мгновение классический (схема по центру), затем HUD. Замер — запись экрана; закрывает
  находку 3, если мгновение видно глазом, и понижает её до косметики, если нет.
- **Пункт управления посреди езды, шторку сразу закрыть.** Ожидание: машинка стоит (в логе
  `goodbye — stopping`), `session over — stream ends` на том же такте, возврата по крошкам нет
  (`motors.owner` не становится `recovering`), картинка обратно ≤ 1 с с новым `stream` и
  ключевого кадра; «Поиск…» на экране — короче секунды. (Шаги 11–12.)
- **Приложение закрыто из переключателя посреди езды.** Ожидание: `bye` дошёл — машинка стоит,
  `link.timeouts` не растёт, в логе нет `no control frame for >300ms`. Если растёт — 300 мс
  дедлайна не хватает на пути через USB при выгрузке.
- **Лист настроек закрыт раньше 3 с.** Ожидание: `viewer moved to` в логе машинки, `stream` на
  проводе тот же, картинка возвращается ≤ 250 мс с вынужденным ключевым (`key` в первом `view`).
  **Закрыт позже 3 с:** `viewer gone — stream ends`, затем `stream N+1 up`, первый кадр — IDR.
- **Битрейт в настройках при открытом потоке.** Ожидание по коду: изменить ползунок, закрыть лист
  за < 3 с — `video.kbps` в `/status` остаётся прежним; выйти с экрана на > 3 с — новый. Это
  наблюдаемая цена находки 5.
- **Телеметрия против потока на 3000 кбит/с через адаптер.** Уже в bringup («link.rx_hz holds
  10»); для J2 добавить: экран ни разу не уходит в «Поиск…» за минуту езды с картинкой — это
  граница 2,4 с, а не 300 мс, и её не видно с консоли машинки.
- **Геймпад под обязательным мастером** (находка 1) — на живом iOS с контроллером: отклонить
  стик при открытом мастере; ожидание после починки — машинка стоит.


## J3 Потеря связи — как записано в `03-link-loss.md`

| № | Проверка | Ожидаемый результат | Закрывает |
|---|---|---|---|
| С1 | При `connected` выключить машинку, засечь по панели адаптера («Попытка N из 5») длительность одной попытки и момент «Нет сети»; включить машинку через 30 с | По спеке `dongle/car-join` («через полминуты появляется снова — переприсоединяется сам») адаптер должен вернуться в `connected` без `POST /wifi`. По коду ожидается `failed` через ~10 с и никакого переприсоединения — тогда сценарий спеки ложен, а F1 достижим простым выключением-включением | шаг 10 (`?`), F1 |
| С2 | Ехать вперёд, выдернуть адаптер из телефона | Машинка ≤ 320 мс держит последнюю команду, затем едет **назад** по пройденному до 5 с (или до конца движения в окне) и встаёт; пульт в пределах секунды — «нет адаптера»; после возврата адаптера — сеть, гейт, езда без участия пользователя (кроме ожидания) | шаг 12 |
| С3 | Уехать из зоны на ~3 с (или заэкранировать машинку) и вернуться в зону; снять `link.timeouts`, время «Управляет: авто-возврат» и лог адаптера («went unanswered while the station says connected — rejoining») | Без толчка возврат прервался бы первым же кадром в зоне; с толчком — ожидается переприсоединение адаптера и возврат назад ещё на секунды после того, как машинка уже в зоне. Мера перегиба — вес F2 | шаг 9, F2 |
| С4 | Два телефона с двумя адаптерами, оба на экране вождения | Осцилляция с периодом ~3 с (п. 11) плюс на каждом периоде лог переприсоединения у вытесненного адаптера; возврата по крошкам ни разу (`motors.owner` никогда `recovering`) | шаг 8, F2 |
| С5 | Перезагрузить машинку кнопкой сброса на ходу (без OTA) | Адаптер: толчок на ~2-й секунде, «Поиск сети», затем `connected` внутри бюджета; пульт: «поиск» → езда сам, `link.timeouts` = 0 после загрузки; машинка стоит после загрузки (`idle`) | шаг 13 |
| С6 | Видео на пределе USB (`video.bitrate_kbps` выше измеренного потолка ~1,5 Мбит/с), стик отпущен | Телеметрия должна доходить (`relay.to_phone_hz` ≈ 5). Если очередь USB роняет телеметрию — пульт уйдёт в «поиск» на 2,4 с и переоткроет сессию при живой машинке; машинка при этом слышит нули и стоит. Явление — шов J2/J3, здесь только измерить | шаг 6 |
| С7 | Сторож на ходу (из `docs/IDEAS.md` п. 5, никогда не проверялся) — мгновенное выключение телефона на ходу | 300 мс → задний ход по крошкам → стоп; `link.timeouts` = 1; через 10 с `motors.owner: idle`, сессии нет | шаги 1–5 |


## J4 Обновление и откат — как записано в `04-update-rollback.md`

Все — `-viaDongle` с адаптером на Mac или телефон; обе консоли пишут.

| Проверка | Ожидаемый результат | Закрывает |
|---|---|---|
| Хронометраж обычного OTA машинки при запуске (без смены радио): от `ok` до «Готово», строки адаптера «sends to the car went unanswered … rejoining», было ли событие разрыва станции | По коду: ~55–58 с, «Готово» на грани; «Прошито» при любой задержке. Цель после починки — секунды | F3, окно 60 с |
| Хронометраж двойной загрузки с образом радио (пин поднят или C6 вручную на старой версии): от `ok` до второй строки `radio firmware …`, состояние адаптера (`connected`/`searching`/`failed`) в каждый момент | Доставка < 50 с; адаптер не доходит до `failed`; пульт — «Готово» или «Прошито» с версией | шаг 7, F3, F4 |
| Адаптер в `failed` во время принудительного обновления машинки (передержать машинку выключенной после `ok` дольше бюджета адаптера) | Сегодня: «Жду, пока устройство выйдет на связь» навсегда, после «Прошито» — экран без кнопок. После починки: экран сам просит адаптер присоединиться | F4, F1 |
| Реальный откат (образ с паникой до `mark_app_valid`, из `docs/IDEAS.md`): OTA → откат → «откат» на экране → «Повторить» | Сегодня: «Требуется обновление» без кнопок. После починки F1: повторная заливка того же образа с предупреждением | F1 |
| После отката опубликовать чинящий выпуск, перезапустить приложение, «Повторить» на «Обновление машинки откатилось» | Сегодня: снова «откатилось» — кабель. После починки F2: «обновление» и прошивка чинящего выпуска | F2 |
| Отмена заливки в последние доли секунды (или обрыв USB сразу после 100 %) — машинка приняла тело, `ok` потерян | Сегодня: повторная заливка того же образа и ложный «откат». После починки F5: «Готово» по свежему `/version` | F5 |
| Удержание «нет выпуска для машинки» (снять `ajmiddlecar.bin` с последнего релиза) три минуты | Сегодня: через ~2 минуты «Нет интернета», `curl api.github.com/rate_limit` с того же адреса — `remaining: 0`. После починки: «нет выпуска» всё время | F6 |
| `curl -X POST --data-binary @<4 КБ нулей> 192.168.7.1:8080/ota` и то же с обрезанным настоящим образом | Сегодня: «connection reset» без тела. После починки: `400 not_firmware` конвертом | F7 |
| Принудительный экран без ленты с кэшем старше тега (выключить интернет между «Проверяю обновления» и заливкой, в кэше — предыдущий выпуск) | Наблюдение: лишний цикл заливки предыдущего выпуска, потом `.updating` снова | шаг 13 (не находка) |


## J5 Конфигурация и калибровка — как записано в `05-config-calibration.md`

| Проверка | Ожидаемый результат | Закрывает |
|---|---|---|
| `POST /config` с изменённым `trim`, `ramp`, `recovery` → перезагрузка машинки → `GET /config` | принятые значения на месте; `storage.reset_at_boot:false` | шаг 8 (`?` без мока) |
| Пройти мастер → перезагрузка → `GET /calibration`, телеметрия | та же таблица, `calibrated:true` | шаг 10, спека «Перезагрузка после сохранения» |
| Прошивка с поднятой версией NVS (или `idf.py erase-flash` только NVS) → загрузка | `/status`: `storage.reset_at_boot:true`, `motors.calibrated:false`; `GET /config` — умолчания контракта; пульт: обязательный мастер открылся, но экраны настроек показывают прежние значения из кэша (до починки `AJM-99`) | шаг 9, `AJM-99` |
| `POST /config {"ramp":{"rise_ms":2000}}` → мастер → «Крутить», машинка на полу и на весу | колесо заметно поворачивается или нет; при «нет» — `AJM-109` растёт в весе | шаг 13, `AJM-109` |
| Платы ШИМ без питания, машинка некалиброванная → пульт | обязательный мастер; «Крутить» → `200` через 0,6 с, ни одно колесо не крутится, колёса доступны для выбора (до починки `AJM-100`) | шаг 16, `AJM-100` |
| Мастер при живой сессии, стик отпущен: «Крутить» | телеметрия на 0,6 с несёт `owner:"calibration"`, `link.timeouts` не растёт, после `200` колесо стоит, `owner` — `remote`/`idle` | шаги 13–14 |
| Езда по крошкам (обрыв → возврат) и «Крутить» из второго клиента (`curl`) посреди возврата | возврат прерван, импульс отработал, машинка стоит, остаток пути не отматывается | шаг 15 |
| Мастер до «Сохранить» → выключить адаптер до ответа → включить | сессия заново; если машинка успела принять — `calibrated:true`, мастер не открывается; иначе — открывается с шага 1 | шаг 18 |


## J6 Отказы железа — как записано в `06-degraded-hardware.md`

Каждая строка — с ожидаемым результатом; какие issues закрывает — в скобках.

- **Платы включить после загрузки.** Машинка загружена с обесточенными PCA9685 (`motors.bus: down`,
  лог `motor bus did not come up`); подать питание. Ожидание по коду: `PCA9685 boards are up` в
  пределах секунды, `/status` → `motors.bus` остаётся `down`, HUD — «Драйвер моторов не
  отвечает», пока не двинуть стик; после починки — `ok` без команды (AJM-91).
- **Провод I2C выдернуть на ходу** (платы под питанием, колёса в воздухе). Ожидание: `bus: down`
  и плашка HUD в пределах секунды, `I2C transaction timeout`/`resetting the I2C bus` ~раз в секунду,
  колёса **продолжают** крутиться на последней скважности; вернуть провод — `ok` следующим
  тактом, без перезагрузки; сброс P4 при выдернутом проводе колёс не останавливает (J7).
- **Моторы рядом с камерой.** `streaming` 60 с при езде: `motors.bus: ok`, `link.rx_hz` 10,
  `link.timeouts` не растёт, джиттер писателя (лог/осциллограф SCL) — уже в `bringup.md`
  → Video, повторено ради `00-bench.md` (Gotchas 11).
- **Шлейф камеры вытащить при `streaming`.** Ожидание по коду: `capture failed — stream over`
  ≤ 0,5 с, `video.state: idle`, HUD «Нет картинки / Камера ждёт», дальше по строке(ам) ошибок
  каждые 1–1,5 с; какой вызов отказывает — `STREAMON` или DQBUF; вернуть шлейф — оживает ли поток
  без перезагрузки (сенсор без reset-пина) (AJM-94).
- **Бюджет радио исчерпан.** C6 перепрошить старым образом, трижды сорвать доставку (снять
  питание в момент `flashing the radio`); четвёртая загрузка: `giving up and booting on`,
  `/status` → `mismatch`, загрузка на ~5 с дольше, пульт — ничего (AJM-92). Затем `POST /ota`
  выпуска с другим пином радио: по коду — снова `giving up`, ни одной попытки (AJM-97);
  `attempts` сбрасывается только совпадением: стендовая перепрошивка до `expected` → `ok`.
- **Новая плата из коробки.** Заводской образ C6 без RPC 350: `radio.state: unavailable`,
  радио не прошивается машинкой, +5 с каждой загрузки — подтвердить, что путь по
  `firmware/car/modem/README.md` остаётся единственным (шаг 9; замысел, не дефект).
- **Адаптер на питании от COM, кабель хоста выдернут.** Ожидание спеки: `usb.state: down`
  и «Нет хоста» на панели; по коду (`TINYUSB_DEFAULT_CONFIG`, без VBUS-сенса) — под вопросом:
  зафиксировать, что показывает `/status` по COM-консоли и панель через 1 с, 10 с, и после
  возврата кабеля.
- **Самопереподключение линка NCM.** Спровоцировать stall (bench-поток `CONFIG_DONGLE_VIDEO_BENCH`
  выше 8128-байтного блока или `libusb` пауза хоста): адаптер логирует `usb link stalled —
  re-attaching`, телефон видит переподключение, пульт — «Нет адаптера» → лестница → экран
  вождения без рук; машинка за это время отъезжает по крошкам и останавливается (J3).
- **Некалиброванная машинка с обесточенными платами.** `/config` без калибровки, платы без
  питания, экран вождения: мастер открыт, «Крутить» → `200`, ничего не крутится, закрыть нельзя
  (AJM-93).
- **Адаптер без панели / панель с зависшей шиной.** Загрузка без панели: `/status` отвечает,
  `relay.to_car_hz` ненулевой при потоке; шина панели закорочена (SDA на землю): задержка
  `display_start` ≤ единиц секунд, реле и HTTP не задеты, `dongle up` в журнале, mark-valid
  прошёл.


## J7 Безопасность под током — как записано в `07-safety.md`

| # | Что сделать | Ожидаемый результат | Закрывает |
|---|---|---|---|
| 1 | Осциллограф на любом выходе PCA9685 (или на входе BTS7960), `mix 0.5 0` с консоли, нажать RESET на P4 на ходу. Измерить от сброса до первого нуля на выходе | Сегодня: ШИМ идёт сквозь сброс и гаснет на `pca9685_zero_all()` в `app_main`; число — окно п. 1. Если оно ≥ сотен мс — решение в проводке `~OE`, не в коде | п. 1, F3 |
| 2 | На ходу (поток пульта, `throttle 0.5`) коротко замкнуть SDA на землю на ~2 с, затем отпустить | В пределах такта `motors.bus: "down"`, колёса держат последнюю скважность (спека: принято), лог `PCA9685 write failing … resetting the I2C bus` раз в секунду, после снятия — `ok` и колёса следуют стику | п. 17 |
| 3 | Удерживая SDA на земле, нажать RESET; отпустить SDA через ~5 с после старта | Сегодня без прошивочного сброса шины: либо драйвер IDF чистит её сам и через ≤2 с лог `PCA9685 boards are up` — тогда F1 понижается до «пути расходятся»; либо `init` падает по таймаутам до батареи — тогда F1 подтверждён как urgent | п. 18, F1 |
| 4 | Платы без питания при старте, `mix 0.5 0` с консоли (захват `console`), подать питание на платы, стик не трогать | `PCA9685 boards are up`; колёса **стоят** (регистры пусты — F2 здесь не проявится, это проверка ✓-части п. 18); `motors.bus` — см. AJM-91 | п. 18 |
| 5 | Десять нажатий «Крутить» в мастере при живом потоке пульта с ненулевым стиком | Каждый импульс: только одна пара 0,6 с; `link.timeouts` не растёт; после импульса колёса возвращаются к стику в пределах 100 мс без рывка (разгон от нуля) | п. 10 |
| 6 | Уйти в фон на ходу; отдельно — выключить WiFi на ходу | Фон: стоп < 150 мс, `timeouts` без изменений (замысел «How we will know»); WiFi: возврат назад и стоп, `timeouts` +1 (`docs/bringup.md` → «Watchdog», открыт с рождения семьи) | п. 6, 7 |
| 7 | `mix 1 0` с консоли, затем `hello` + поток нулей с пульта, затем `bye` | Поток отбирает колёса у консоли (стоп), после `bye` — `idle`; `mix` без `mix 0 0` больше ничего не держит | п. 11 |
| 8 | Видеопоток включён, поток пульта 10 Гц, стик на половине: смотреть `link.rx_hz` и плавность разгона по ШИМ | `rx_hz` 10 ± 1, шаги разгона равные (`isp_task` prio 11 не растягивает такт писателя) — открытый вопрос `CLAUDE.md`, gotcha 11 | п. 4, 20 |


## J8 Что скрывает мок — как записано в `08-mock-blind-spots.md`

| Проверка | Ожидаемый результат | Закрывает |
|---|---|---|
| `tools/conformance.py http://192.168.7.1` на машинке со стендовой сборкой (`fw` с `-N-gSHA` или `-dirty`) | Сегодня: `FAIL /version.build`; после AJM-108 — зелёный | AJM-108 |
| `POST /ota` образом адаптера (`ajdongle.bin`) на машинку через адаптер | `400 not_firmware` после полной загрузки (`esp_ota_end`), `/version` прежний, `rolled_back:false`, `motors.owner` снова `idle` | AJM-105 (подтверждение стороны машинки) |
| Обрыв загрузки `/ota` на середине с зависшим TCP (не закрытие сокета): `curl --limit-rate` и `kill -STOP` | Через ~30 с — `500 internal`, актуатор отпущен, следующий `drive` едет (`car/ota-and-rollback` → «Остановившаяся загрузка не держит машинку») | вердикт `?` шага 7; мок этого не изображает |
| Прошивка с откатом (битый образ или `--rollback`-подобный сценарий на плате) при подключённом пульте | Экран «Не удалось / откат» появляется после переподключения — путь `sawOffline → та же версия`, недостижимый на моке | AJM-103 (сторона пульта) |
| `conformance_rt.py 192.168.7.1:4210` с новыми легендами AJM-117 (машинка на подставке) | `link.timeouts` +1 после тишины, `recovering → idle` после короткого хода, повтор `hello` не сбрасывает gate | AJM-117 |
| `conformance_video.py 192.168.7.1` с легендами AJM-118, два телефона для вытеснения | Чужой sid — тишина; `bye` — тишина в пределах такта; `/status.video.state` `streaming`/`idle`; при вытеснении поток кончается (после AJM-66) | AJM-118, стенд 5.2 `ajm-64-known-fixes` |
| Гистограмма интервалов между чанками ключевого кадра с машинки | Медиана ≈ 3 мс (струйка), не 0 | AJM-118 (легенда 5) |
