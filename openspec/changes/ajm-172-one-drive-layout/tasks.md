# Tasks

Пункт — один воркер, один PR. Общая проверка каждого: `openspec validate ajm-172-one-drive-layout --strict`; `CONFORMANCE=required tools/test-all.sh` (хост-тесты `drivelayout`/`drivemode`); `cd app && xcodegen generate && xcodebuild build -scheme AJMiddleCar -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/ddata-middle`. Поведение — по дельтам `specs/app/drive-hud/spec.md` и `specs/app/drive-session/spec.md`, решения — `design.md`. 1.1 → 1.2 последовательно: оба трогают `DriveView.swift`. Симулятор и снимки — у PM (2.1).

## 1. Одна раскладка

- [ ] 1.1 Убрать классическую раскладку: `DriveMode` удалить, `DriveScreenState` = `{watching, inputAllowed}` (`DriveModeRule.state`/`videoTap` — те же сигнатуры), `DriveView.body` — один `GeometryReader` с `DriveLayout` (ветка `classic` и её органы удаляются); `VideoView` смонтирован только при подтверждённом `enabled: true`, плашка «Нет картинки» — только при живом окне; кадр галереи «Drive classic (video off)» → «Drive video off»; док-комментарии `DriveMode.swift`/`DriveView.swift` без упоминаний классики; проверить: `app/tests/drivemode/main.swift` переписан на новое состояние (домен не прочитан / `false` → `watching: false`, `true` → `watching: true`, под листом — `inputAllowed: false` при любом домене), `tools/test-all.sh` зелёный, `xcodebuild` собирается; источники: `app/AJMiddleCar/DriveView.swift` (`body`, `hud`, `classic`, `noPicture`), `DriveMode.swift`, `GalleryView.swift:148-156`, `openspec/specs/app/drive-hud/spec.md` (AJM-173)
- [ ] 1.2 Бар из трёх кнопок (после 1.1): новый `app/AJMiddleCar/ControlBar.swift` — капсула 3 × (44 × 32), фон `segOff`, обводка `line`, r10, разделители `line`; `TricksControl` в форме сегмента (глиф 15 pt, кольцо 26 pt/2 pt вокруг глифа, карточка вниз хвостом вверх, `CardEdge` удалить; параметры `running`/`startedAt`/`onSelect`/`onStop`/`debugOpen`/`debugRingProgress` не меняются); сегменты видео и настроек — прежние кнопки без своей рамки, отказ видео — обводка сегмента `warn` на 650 мс, настройки `disabled` под мастером; верхний ряд `DriveView`: `SchemeToggle` + 8 pt + `ControlBar`; `DriveLayout.tricks` удалить; `CLAUDE.md` абзац «The drive screen is a HUD…» — одна раскладка, бар, ссылка на `openspec/specs/app/drive-hud`; проверить: `app/tests/drivelayout/main.swift` без проверки `tricks`, остальные точки те же; `tools/test-all.sh` зелёный; `xcodebuild` собирается; `grep -rn 'cardEdge\|CardEdge\|lay.tricks' app/` пуст; источники: `app/AJMiddleCar/TricksControl.swift`, `DriveLayout.swift`, `DriveView.swift` (`videoButton`, `gearButton`, верхний `HStack`), `SchemeToggle.swift` (образец токенов), `Theme.swift` (`segOff`, `line`) (AJM-174)

## 2. Проверка глазами

- [ ] 2.1 (PM) Снимки галереи в симуляторе после слияния 1.2: кадры «Drive arcade», «Drive tank», «Drive warning», «Drive bus/ctl warning», «Drive tricks open», «Drive video off» (`-gallery -galleryIndex 48…53`, `sips -r -90`); сверить с макетом брейншторма: ни один орган не сдвигается между аркадой/танком и видео вкл/выкл, карточка трюков под баром не выходит за экран, кольцо хода читаемо; расхождения — issues в проект, не правки в main
