# Name,     Type, SubType,  Offset,   Size
# 16 MB flash. The two app slots are sized for where the P4 is going (camera + H.264, LVGL
# display, lidar all live in this image), not for what it does today: changing the table
# later means a wired reflash. `storage` is a reservation, not a filesystem choice —
# nothing mounts or formats it; unallocated flash simply cannot be claimed later.
nvs,        data, nvs,      0x9000,   0x6000
otadata,    data, ota,      0xf000,   0x2000
phy_init,   data, phy,      0x11000,  0x1000
ota_0,      app,  ota_0,    0x20000,  0x400000
ota_1,      app,  ota_1,    0x420000, 0x400000
storage,    data, fat,      0x820000, 0x7E0000
